diff --git a/autoware_launch/config/planning/scenario_planning/common/common.param.yaml b/autoware_launch/config/planning/scenario_planning/common/common.param.yaml
index 8d2228d..f307790 100644
--- a/autoware_launch/config/planning/scenario_planning/common/common.param.yaml
+++ b/autoware_launch/config/planning/scenario_planning/common/common.param.yaml
@@ -13,7 +13,7 @@
 
 /**:
   ros__parameters:
-    max_vel: 4.17           # max velocity limit [m/s]
+    max_vel: 13.3           # max velocity limit [m/s]
 
     # constraints param for normal driving
     normal:
diff --git a/vehicle/sample_vehicle_launch/sample_vehicle_description/config/vehicle_info.param.yaml b/vehicle/sample_vehicle_launch/sample_vehicle_description/config/vehicle_info.param.yaml
index 3e0f10d..2174964 100644
--- a/vehicle/sample_vehicle_launch/sample_vehicle_description/config/vehicle_info.param.yaml
+++ b/vehicle/sample_vehicle_launch/sample_vehicle_description/config/vehicle_info.param.yaml
@@ -1,12 +1,12 @@
 /**:
   ros__parameters:
-    wheel_radius: 0.383 # The radius of the wheel, primarily used for dead reckoning.
+    wheel_radius: 0.354 # The radius of the wheel, primarily used for dead reckoning.
     wheel_width: 0.235 # The lateral width of a wheel tire, primarily used for dead reckoning.
-    wheel_base: 2.79 # between front wheel center and rear wheel center
-    wheel_tread: 1.64 # between left wheel center and right wheel center
-    front_overhang: 1.0 # between front wheel center and vehicle front
-    rear_overhang: 1.1 # between rear wheel center and vehicle rear
+    wheel_base: 2.944 # between front wheel center and rear wheel center
+    wheel_tread: 1.63 # between left wheel center and right wheel center
+    front_overhang: 0.864 # between front wheel center and vehicle front
+    rear_overhang: 1.04 # between rear wheel center and vehicle rear
     left_overhang: 0.128 # between left wheel center and vehicle left
     right_overhang: 0.128 # between right wheel center and vehicle right
-    vehicle_height: 2.5
-    max_steer_angle: 0.70 # [rad]
+    vehicle_height: 1.507
+    max_steer_angle: 0.48 # [rad]
