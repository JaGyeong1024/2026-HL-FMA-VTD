# h1_red_green 과 같되 VTD 출발 데드밴드를 0.6 m/s² 로 가정 (실측 전 가정치). 재출발 교착 D1 재현 시도.
# 사전테스트 관측: 정지 후 재출발 못 함. 스무더 engage 0.25/0.5 → 명령 ≈0.42 m/s² 로 추정(복귀 분석).
ROUTE=$HOME/hlfma/route/route_pretest_2.csv
MOCK_ARGS="--clock move --tl 1 --tl-at 55:3 --start-deadband 0.6"
RUN_SEC=95
METRIC_ARGS="--mark green"
EXPECT="녹색 10 s 내 재출발. baseline 은 교착(재출발 없음) 이 기대 실측"
