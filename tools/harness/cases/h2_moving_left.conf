# 정지차 + 좌측 차선 뒤에서 13 m/s 로 오는 차(ego 첫 이동 12 s 후 시작점 후방 40 m 에서 등장) → 설계상 양보 후 우회
ROUTE=$HOME/hlfma/route/route_pretest_2.csv
MOCK_ARGS="--clock move --tl 3 --obj 7,90,0,0,0,4.5,1.8 --mover 8,-40,3.2,0,13,4.5,1.8,12,40"
RUN_SEC=60
METRIC_ARGS="--obj 7 --lanes"
EXPECT_DETOUR=yes
