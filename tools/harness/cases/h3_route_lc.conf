# 경로상 필수 차선변경: route_node 로그의 '차선변경:' 계획 수 vs 실제 lanelet 열에서 관측된 이웃 전환 수
ROUTE=$HOME/hlfma/route/route_pretest_2.csv
MOCK_ARGS="--clock move --tl 3"
RUN_SEC=150
METRIC_ARGS="--lanes"
EXPECT="계획된 차선변경이 있으면 전부 실행(lane_changes >= 계획 수), 취소 0"
