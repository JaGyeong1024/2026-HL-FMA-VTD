# lane_change 단독 + 복귀 여유 64 m — "복귀에 얼마나 필요한가" 측정용.
#   자차 차선(A) 정지차 4대를 20 m 상류로: -84.4 / -77.4 / -70.3 / -64.1
#   네 대가 전부 점선 구간 15379(-92.6..-60.4) 안에 들어간다. A 는 -64.1 이후 뚫려 교차로까지 64 m.
#   (30 m 를 옮겼더니 첫 대가 실선·단일차선 구간 15719 로 들어가 차선변경 자체가 불가능했다 — 잘못된 시험)
#   원래 배치(복귀 여유 44 m)에서는 복귀 실패. 사유는 충돌이 아니라(선행·후행 0건)
#   후보 생성 고갈 + 종점 정지 삽입이었다(h4_lc_only_0908_104507).
# avoidance 는 꺼져 있다. 실선 미침범(항목 6)·방향지시등(항목 13) 때문에 lane_change 로만 간다.
REROUTE="2=15414 3=15220 4=14890"
ROUTE=$HOME/hlfma/route/route_pretest_2.csv
MOCK_ARGS="--x 242.572 --y 146.853 --hdg -1.2430 --z 37.200 --clock move --tl 3 \
--obj-abs 10,287.880,17.269,40.050,-1.2228,0,4.39,1.81 \
--obj-abs 11,290.288,10.670,40.240,-1.2183,0,4.39,1.81 \
--obj-abs 13,292.719,4.106,40.430,-1.2138,0,4.39,1.81 \
--obj-abs 14,294.917,-1.629,40.580,-1.2095,0,4.39,1.81"
RUN_SEC=150
METRIC_ARGS="--lanes"
EXPECT="lane_change 만으로 A->B 우회 후 B->A->포켓(14611) 복귀, 교차로 18858 통과"
