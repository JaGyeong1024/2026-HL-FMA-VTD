# 자차 차선(우측) 정지차 + 좌측 차선 비어 있음 + 녹색 → 설계상 우회 대상
ROUTE=$HOME/hlfma/route/route_pretest_2.csv
MOCK_ARGS="--clock move --tl 3 --obj 7,90,0,0,0,4.5,1.8"
RUN_SEC=60
METRIC_ARGS="--obj 7 --lanes"
EXPECT_DETOUR=yes
