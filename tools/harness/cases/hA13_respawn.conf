# 주행 중 리스폰(ego 첫 이동 25 s 후 시작점으로) → 경로 재설정·자율주행 복귀·재출발
ROUTE=$HOME/hlfma/route/route_pretest_2.csv
MOCK_ARGS="--clock move --tl 3 --respawn-at 25"
RUN_SEC=80
METRIC_ARGS="--mark respawn"
EXPECT="리스폰 30 s 내 재출발(restart_after_mark_s<=30), 종료 시 route=2 mode=2, 경로 루프 없음"
