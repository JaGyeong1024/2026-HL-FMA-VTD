# 우회 전반부 단독 검증 — "처음부터 느리게" 가설.
# 0908 실측(h4_pocket_ext2_0908_072214, external·avoidance 끔, preferred 전 구간 C):
#   lane_change_right 가 승인(RUNNING)까지 갔으나 0.1초 만에 이탈하며 진동, 후보 11개 중 10개가
#   "sampled path is not safe", 결국 "next lc terminal" 로 정지 → obstacle_stop 0.22m 고착.
#   원인은 속도다. v=12.3 m/s 에서 차선변경 1회 길이 lc_len=37.9 m.
#   A→B→C 두 번을 첫 정지차(-64.4 m) 전에 끝내야 하는데 가용 거리는 약 50 m → 76~80 m 필요, 불성립.
#   lane_planner 가 3.2 m/s 를 걸긴 했으나 t=26s(이미 결정 구간 안)라 늦었다.
# 이 판은 engage 전에 상한을 걸어 처음부터 느리게 간다. 3.2 m/s 면 1회 약 10~16 m, 2회 32 m < 50 m.
# LANE_PLAN=false 로 돌린다 — 속도 결정 주체를 하나로 두기 위해(lane_planner 도 상한을 건다).
REROUTE="2=15449 3=15233 4=14960 5=14677"
VEL_LIMIT=3.2
ROUTE=$HOME/hlfma/route/route_pretest_2.csv
MOCK_ARGS="--x 242.572 --y 146.853 --hdg -1.2430 --z 37.200 --clock move --tl 3 \
--obj-abs 10,295.027,-1.515,40.585,-1.1995,0,4.39,1.81 \
--obj-abs 11,297.467,-8.447,40.780,-1.1831,0,4.39,1.81 \
--obj-abs 12,296.979,-15.850,40.955,-1.1647,0,4.39,1.81 \
--obj-abs 13,300.438,-14.665,40.962,-1.1639,0,4.39,1.81 \
--obj-abs 14,302.926,-20.021,41.119,-1.1453,0,4.39,1.81 \
--obj-abs 15,299.745,-21.288,41.114,-1.1458,0,4.39,1.81"
RUN_SEC=200
METRIC_ARGS="--lanes"
EXPECT="A→B→C 우측 2회 완료 후 정지차 6대를 지나 C 로 교차로 도달"
