# h4_pocket 과 같은 장면. 다른 점: 우회를 '웨이포인트'로 표현해 루트에 넣는다.
#   external_request_lane_change_* 를 쓰지 않고, 경로 기반 lane_change_left/right 만으로 회피가 되는지 본다.
#   (external 은 LaneChangeModuleType 이 NORMAL 이 아니라 scene.cpp:383 early return 에 걸려
#    깜빡이 타이머가 안 쌓이고 준비시간이 4.0 s 로 고정된다 — 0908 코드 확인.)
# 추가 웨이포인트 2개 (route_h4_wp.csv seq 2·3):
#   seq2 (284.076,8.385)   = C 차선 종방향 -78 m. C·D 만 완전히 비어 있어 정지차 6대를 전부 우회할 수 있는 유일한 차선.
#   seq3 (318.853,-45.791) = 좌회전 포켓 P 종방향 -14 m. 포켓 진입을 루트로 강제한다.
# 관전 포인트: 복귀 구간 — 정지차 마지막이 -44.1 m, 교차로가 0 m. 44 m 안에 C→B→A→P 좌측 3회가 필요하다.
ROUTE=$HOME/hlfma/route/route_h4_wp.csv
MOCK_ARGS="--x 242.572 --y 146.853 --hdg -1.2430 --z 37.200 --clock move --tl 3 \
--obj-abs 10,295.027,-1.515,40.585,-1.1995,0,4.39,1.81 \
--obj-abs 11,297.467,-8.447,40.780,-1.1831,0,4.39,1.81 \
--obj-abs 12,296.979,-15.850,40.955,-1.1647,0,4.39,1.81 \
--obj-abs 13,300.438,-14.665,40.962,-1.1639,0,4.39,1.81 \
--obj-abs 14,302.926,-20.021,41.119,-1.1453,0,4.39,1.81 \
--obj-abs 15,299.745,-21.288,41.114,-1.1458,0,4.39,1.81"
RUN_SEC=120
METRIC_ARGS="--lanes"
EXPECT="C 차선으로 우회 후 포켓(15194/14855/14611) 진입, 교차로 18858 통과"
