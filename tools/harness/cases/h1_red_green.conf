# 적신호 정지 → 녹색 → 재출발 (재출발 교착 D1 검출). 첫 정지선은 시작점 222 m 앞 (route_pretest_2)
ROUTE=$HOME/hlfma/route/route_pretest_2.csv
MOCK_ARGS="--clock move --tl 1 --tl-at 55:3"      # ego 첫 이동 55 s 후 녹색 (정지선 도달 ~35 s → 20 s 대기)
RUN_SEC=95
METRIC_ARGS="--mark green"
EXPECT="정지선 앞 정지(stopped_before_mark=True) 후 녹색 10 s 내 v>1 m/s (restart_after_mark_s<=10)"
