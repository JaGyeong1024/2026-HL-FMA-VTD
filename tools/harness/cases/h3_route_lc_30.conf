# 같은 경로, 30 km/h 상한 (max_velocity_default 로 제한) — 저속에서의 차선변경 길이·완료
ROUTE=$HOME/hlfma/route/route_pretest_2.csv
MOCK_ARGS="--clock move --tl 3"
RUN_SEC=200
VEL_LIMIT=8.3
METRIC_ARGS="--lanes"
EXPECT="계획된 차선변경 전부 실행"
