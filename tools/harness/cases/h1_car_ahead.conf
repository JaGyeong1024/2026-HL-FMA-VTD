# 자차 차선 정지차 앞 정지: 정지거리·최대감속. (baseline: 회피 없음, 5 m 여유 정지가 기대)
ROUTE=$HOME/hlfma/route/route_pretest_2.csv
MOCK_ARGS="--clock move --tl 3 --obj 7,90,0,0,0,4.5,1.8"   # 시작점 전방 90 m, 자차 차선(직선 구간)
RUN_SEC=45
METRIC_ARGS="--obj 7"
EXPECT="정지(obj_gap_at_stop 2~8 m), 객체 통과 없음(obj_passed=False), 최대감속 기록"
