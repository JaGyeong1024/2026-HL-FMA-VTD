# 분리 시험: "경로가 3차선(C)을 가리키면 자차가 C 로 가는가" — 그것만 본다.
#   장애물 없음, lane_planner 없음(LANE_PLAN=false). 순수 Autoware + preferred 재지정뿐.
#   seg2~5 의 preferred 를 전부 C 로 둔다. 자차는 A 에서 출발하므로 A→B→C 두 칸이 필요하다.
#   기대: C lanelet(15449/15233/14960/14677) 이 lanelet 열에 나타나야 한다.
REROUTE="2=15449 3=15233 4=14960 5=14677"
ROUTE=$HOME/hlfma/route/route_pretest_2.csv
MOCK_ARGS="--x 242.572 --y 146.853 --hdg -1.2430 --z 37.200 --clock move --tl 3"
RUN_SEC=140
METRIC_ARGS="--lanes"
EXPECT="C 차선(15449/15233/14960/14677) 도달"
