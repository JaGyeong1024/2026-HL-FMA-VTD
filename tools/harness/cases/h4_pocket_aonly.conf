# 문제 분리용: 자차 차선(A) 정지차 4대만. 우측 차선 B 는 비운다.
#   0908 실측에서 모든 모듈이 "목표 차선에 정지차가 예측 경로 안에 있다"로 거부했다.
#   B 를 비우면 A→B 가 열리므로, 남는 실패는 '연속 차선변경' 능력 문제로 분리된다.
#   대회장에서는 6대 봉쇄보다 이쪽이 더 있을 법한 상황이기도 하다.
# 객체는 h4_pocket 과 같은 실측 좌표에서 B 차선의 obj12(-50.3)·obj15(-44.1)만 제외.
ROUTE=$HOME/hlfma/route/route_pretest_2.csv
MOCK_ARGS="--x 242.572 --y 146.853 --hdg -1.2430 --z 37.200 --clock move --tl 3 \
--obj-abs 10,295.027,-1.515,40.585,-1.1995,0,4.39,1.81 \
--obj-abs 11,297.467,-8.447,40.780,-1.1831,0,4.39,1.81 \
--obj-abs 13,300.438,-14.665,40.962,-1.1639,0,4.39,1.81 \
--obj-abs 14,302.926,-20.021,41.119,-1.1453,0,4.39,1.81"
RUN_SEC=150
METRIC_ARGS="--lanes"
EXPECT="A 의 정지차 4대를 비켜 지나 좌회전 포켓(15194/14855/14611) 진입 후 교차로 18858"
