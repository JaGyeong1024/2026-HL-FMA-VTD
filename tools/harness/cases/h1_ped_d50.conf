# 길가 보행자 급출발 — 거리 트리거: ego 가 보행자 위치 50 m 앞에 왔을 때 우측 5.5 m 에서 2.5 m/s 로 차선 방향 횡단(8 s 후 소멸).
# 판정: 충돌 없음 필수. 정지 가능(−2.5 m/s² 정지거리+반응 ≈ 45 m@13 m/s)이면 정지, 아니면 통과. 결과 = 정지/통과/충돌 + 최소거리·속도
ROUTE=$HOME/hlfma/route/route_pretest_2.csv
MOCK_ARGS="--clock move --tl 3 --mover 9,85,-5.5,90,2.5,0.6,0.6,d:50,8"
RUN_SEC=40
METRIC_ARGS=""
EXPECT="충돌 없음; 정지 가능 거리면 정지, 아니면 통과"
