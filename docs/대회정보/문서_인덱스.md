# 문서 인덱스 — HL FMA 프로젝트

여기부터 보면 모든 문서·자료 위치를 알 수 있음. (2026-09-03 정리)

## 활성 문서 (루트, .md) — 항상 최신 유지
| 문서 | 내용 |
|---|---|
| `CLAUDE.md` | 세션 규칙 (시작·종료 절차, 접속, 작업 원칙) |
| `대회정보.md` | **통합 맥락** — 대회 스펙·통신·맵·차량·센서·평가·인프라·결정사항 (세션 시작 시 먼저 읽기) |
| `todo0910.md` | **최신 핸드오프** — 0910: 시나리오 2 우회·복귀 안정화. 플래너 SIGABRT 크래시(8/14→0), 차선변경 완료 판정 교착 해소, 주행가능영역 노선 차로 포함, 경로 급변 9→1회, 누적 220→600 m. 새 문제: 방향지시등 미점등(회귀), 궤적 무효 판정, 복귀 중 감속 |
| `todo0909.md` | 0909 핸드오프 |
| `todo0908.md` | 0908 핸드오프 — 한 것 / 할 것 / 기술부채. 0908: 좌회전 포켓 진입 실패의 근본 원인(모듈 승인 축출·RTC 서비스명·깜빡이 타이머)과 남은 벽(RSS 앞차·RUNNING 고착) |
| `런타임_노드그래프.md` | 실측 런타임 노드·입출력·연결. **최종 궤적=`/planning/trajectory`**(scenario_planning/trajectory 아님) 명시 |
| `수정내역_0906.md` | **우리가 무엇을 고쳤나** — 설정 4건(커밋)·업스트림 패치 2건·신규 노드 1건의 이유·근거·검증상태. 처음 보는 사람용. 재현 절차 포함 |
| `주행리뷰_및_노드감사_0903.md` | 첫 자율주행 성공 회고 — 근본원인 4겹·오해 7건·능력 현황·노드별 대회환경 감사·검증계획 |
| `autoware_통합_검토_0901.md` | Autoware 통합 상세 (초기 검토) |
| `개발계획_0902.md` | 개발 계획 (규정용/성적용 기능 분리 등) |

## 1차자료/ — 원본 참고자료 (수정하지 않음)
| 자료 | 내용 |
|---|---|
| `2026 HL FMA 대회 안내문.pdf` / `...사전 테스트 안내문.pdf` | 주최측 공식 안내문 (당일 진행·평가·채점 기준의 원천) |
| `인터페이스 API.xlsx` | VTD↔참가자 통신 필드 정의 (ego pose·objects·신호등 / steer·accel·turn) |
| `게시판 QnA/` | Redmine 질의응답 스크린샷 + `QnA_정리.md` 전사 |
| `VTD 메뉴얼/` | VTD User/ScenarioEditor Manual, 교육자료 PDF |
| `실습파일/` | 시나리오·통신예제 등 실습 배포본 |
| `교육/`, `8. 교육 260813/` | 교육 자료 |
| `대회설명회 슬라이드/` | 대회 설명회 슬라이드 |
| `TCP, UDP 통신 예제/` | 레퍼런스 수동 제어기(`tcp_manual_controller.py`) 등 |
| `센서인터페이스_역분석/` | 팀 역분석: `07-sensor-interface.md`(카메라 RTSP·LiDAR UDP 규격), `08-ros2-sensor-bridge.md`(ROS2 센서브리지 설계) |
| `autoware_universe.png` | Autoware universe 표준 노드 다이어그램 (참조도) |
| `route_example.csv` | 경로 CSV 예시 형식 |

## 문서보관/ — 이력(대체된 것)
이전 todo(0822·0824·0901·0902), 세션 로그(docs_0821), 원본 정리본(대회설명회·게시판·통신인터페이스 정리), `검토보고_0902/`(맵 감사 등).

## 코드·실행 (제어기 PC)
- 워크스페이스: `~/2026-HL-FMA-VTD/hlfma_ws/` (colcon build). 브리지=`vtd_autoware_bridge`, 경로주입=`vtd_route_node`.
- 실행: `start_autonomous.sh`(기동+route) → `start_hlfma.sh`(engage) → `record.sh`(기록). `rviz.sh` 선택. 경로교체=`route_config.yaml` csv_path.
- 문서 동기화 위치: `~/2026-HL-FMA-VTD/docs/대회정보/` (활성 .md git 커밋).
- 업스트림 autoware 패치: `~/2026-HL-FMA-VTD/patches/` (gitignore된 src라 patch로 보존).

## 접속 (비번 a)
제어기 `ssh a@100.120.213.124`(Tailscale) / `ssh a@192.168.1.55`(공유기). 시뮬 `ssh a@192.168.50.11`(제어기 경유). 대회장 IP: 제어기 192.168.50.10 / VTD 192.168.50.11.
