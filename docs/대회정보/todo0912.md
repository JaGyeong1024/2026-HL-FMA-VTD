# todo 0912 — 대회 당일(09:00) 핸드오프

최신 todo. 이전: `todo0911.md`(D-1 분석·결정). 이 문서는 04:10~06:00 통합·검증 결과와 당일 절차.

## 1. 당일 실행 (main 디렉터리)

- 코드: `~/2026-HL-FMA-VTD` main = JG `3c3b546` (06:00 기준). 빌드는 05:20 시작한 전체 재빌드(9/6 이후 첫 빌드).
- 기동: `./start.sh` (auto engage). 녹화 필요하면 `RVIZ=1 TRACE=0 tools/run_rec.sh <이름> 900` (bag 에서 `/control/*` 전체는 제외해 둠 — CPU).
- 리허설: 06:30 예정 (미검증 3건 확인: 접근 감속 트리거 40 m, 적·황 신호 35 km/h 접근, 진단 timeout 3.0 확장).
- VTD 프레임 저하(≤35 pkt/5 s)가 오면 입력 주기 오류로 비상정지가 짧게 걸린다 — 시뮬 쪽 원인, 스택 조치 없음.

## 2. 오늘(9/12 새벽) 한 것 — 커밋 순

| 커밋 | 내용 | 검증 |
|---|---|---|
| 9c7a720 | NG 백업 2건(차선변경 크래시 수정, 회피 실선 게이트) + 설정(같은 방향 차로만 회피, max_vel 13.0, 경사보상 OFF, run_out 여유 10 m) | 주행 1·2 |
| eeda075 | NG 04:09: 신호 접근 35 km/h 제한(녹색·미할당), 크기 분류기, 보행자 직선 예측, 통과판정 감속 −2.4 | 주행 1·2 |
| 17d60df | 이륜 → BICYCLE 라벨, 자전거 직선 예측, 접근 감속 대상에 자전거, 저속 회피 횡한계 [1.5,1.0,0.5]/저크 [1.0,0.5,0.2] | 주행 2: S자 통과(48 s, 최근접 3.0 m) |
| ddaeffd | 진단 집계기 localization/routing state timeout 3.0 | 주행 2: 해당 STALE 0 |
| 70858bf | 접근 감속 25 km/h, run_out "자차 먼저 지나감/못 섬 → 무시" 해제, 정지 프로파일 main 값 복귀 | 주행 2: 자전거 최근접 8.2 m(이전 0.6) |
| 4c58a1f | 계획 감속 −2.0, PID 제동 하한 −3.0, 지연보상 0.5 | 주행 2: 적색 1차 정지 0.0~0.1 m, 기어감 없음(5/6) |
| 5a7238c | 접근 감속 트리거 60 → 40 m (사용자 결정) | **미검증** |
| 912b11f | 신호 접근 35 km/h 를 적·황에도 적용 | **미검증** |
| fb3586a, 3c3b546 | 1 s 주기 진단 유닛 14개 timeout 3.0 | **미검증**(부류 동일) |
| 5676ea6, cf22054 | run_rec TRACE=0·`/control/*` 제외, stop_analysis | — |

## 3. 데이터로 확정한 원인 (bag: test/0912_044131_full, 0912_050738_full2)

- **적색 2단 정지**: 제동 명령 −3.3~−5.0 에도 실제 감속은 −2.3 m/s² 포화, 응답 시정수 ≈1.2 s. 계획 감속 −2.5 가 한계에 걸쳐 PID 적분이 쌓이고, 목표 반전 후 제동 잔류로 정지선 11 m 전 정지 → 기어감. 계획 감속을 한계 아래(−2.0)로 두니 해소. 고속 접근일수록 심함 → 적·황 35 km/h 접근 추가.
- **자전거 미감속**: 크기로 자전거/오토바이 구분 불가(1.9×0.7×1.8 vs 2.0×0.6×1.7) → MOTORCYCLE 라벨이면 접근 감속(보행자만)·도로이용자 정지(자전거만)·직선 예측(보행자만) 셋 다 안 걸림. 25 km/h 가 튀어나옴(자차 17 m 앞, 4.4 m/s) 대응 상한.
- **비상정지 32회(주행 1)**: ADAPI 상태 진단 STALE(1 s 주기 + timeout 1.0) 절반, 늦은 정지 삽입(run_out 무시조건) → 검증기, CPU(bag 87 %) 로 궤적/제어 주기 저하, VTD 프레임 저하. 주행 2 는 7회(프레임 저하 3, 게이트 STALE 2 → 수정).
- **S자**: 정지차 4대가 각 차로 중앙, 간격 13~19 m. 회피 횡가속 0.5/저크 0.2 로는 한 번 비키는 데 ~15 m 라 불가 → 저속 구간 상향으로 통과.
- **터널 정지차**: 우회 노드가 77 m 부터 14 km/h, 26 m 안 3.6 km/h 로 기어가며 차선변경 대기 → 설계대로. 느리지만 위반 없음.

## 4. 기술부채 / 남긴 것

- 정지선 접근: 20 km/h 로 짧게 접근한 케이스에서 7 m→3 m 2단 정지 1회 남음. 제어기 "정지 진입 거리" 0.49 → 2 m + 신호 정지 여유 1.0 → 0.3 안은 검증 없어 미적용.
- 적색 정지 −0.9 m(정지점 0.9 m 지나침, 선 0.1 m 앞) 1회: 41 m 앞 정차→출발→정지 케이스. 지연보상 0.5 과보상 가능성. 반복되면 0.3 으로.
- 커밋 9c7a720 은 NG 메시지에 우리 설정 변경이 묶여 있음(메시지는 정정).
- main 디렉터리에 9/7자 미커밋 lane_change.param.yaml 이 있었음 → stash 로 보관(`git stash list`), 값은 커밋본에 이미 반영.
- 우회 노드 접근 4 m/s·최저 1 m/s 는 시간 손해(정지차당 ~30 s). 대회 후 6/2 m/s 검토.
- 접근 감속 40 m 는 계산상 여유 없음(튀어나옴 시점 ~31 km/h, 정지거리 21 m > 17 m). 리허설에서 근접 <3 m 나오면 60 으로 되돌릴 것(`bridge.launch.xml` lookahead_distance_m).
- 플래너 디버그 로거 런치 스위치 미적용(필요 시 per-token env 방식).
- 도구: `tools/run_rec.sh`, `score.py`, `stop_analysis.py`(trace 필요), 스크래치 `bag_judge.py`/`bag_stop.py`/`bag_lag.py`/`bag_scurve.py`(제어기 /tmp, 세션 스크래치에 사본).
