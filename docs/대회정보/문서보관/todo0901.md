# 2026-09-01 세션 정리 + TODO

다음 세션 이어가기용. 전체 배경은 `대회정보.md` 먼저. **진행 상황·다음 할 일은 이 문서가 최신** (이전: todo0824.md).

> **이 문서가 세션 핸드오프**: §1·§1B = 이번(9/1) 세션에서 한 것 / §3 = 다음 세션에서 할 것.

**⚠ 다음 세션 시작 시**: Redmine 미답변 질의(정지선 판정 기준, 붉은 노면 좌표/제한속도 값, state 6 적·황 점멸 구분, objects[].id 프레임 간 유지, egoSpeed 제공 여부)에 답변 달렸는지 사용자에게 확인 요청할 것.

---

## 1. 오늘 한 것

### 문서·정보
1. **대회 안내문 PDF 반영** — 본선 9/12(토) 원주, 팀당 20분(연동 포함)·PC 전원OFF 제출·두 팀씩, 구간별 100점·경미 –3/중대 –6, 15개 평가항목 기준치, 순위 산정(완주 그룹 절대 우선). 대회정보.md §7 전면 개편
2. **공식 Q&A 반영** (게시판 QnA/ 스크린샷 3장): ①trafficLightId는 맵 signal id와 무매핑 — state만 사용 ②스쿨존=붉은 노면만(일반 30 노면 무시) ③xodr·API 정보 전량 활용 허용 ④사전테스트는 본체만 지참·경로 제공·반입 시나리오 채점 불가
3. 라이선스 연장 완료 확인 (만료 2026-11-09, 서버 expiry 실측)
4. 맥락 문서 묶음을 제어기 PC(`~/2026-HL-FMA-VTD/docs/대회정보/`)와 시뮬 PC(`~/HLFMA/docs/대회정보/`)에 복사

### 인프라
5. **시뮬 PC IP 복구** — 부팅 시 DHCP 프로필(int)이 잡히던 것을 control(고정) autoconnect로 수정
6. **연구실 이더넷 = 대회장 IP 상시 전환** — 제어기 **192.168.50.10**(공유모드 유지) / 시뮬 **192.168.50.11**. 대회장·사전테스트에서 **케이블만 꽂으면 동작** (당일 조작 제로). Tailscale 라우트 192.168.50.0/24 갱신·승인. lidarDestinationIp=50.10, 도구·브리지 기본 host=50.11. 시뮬 PC 원격: `ssh a@192.168.50.11`

### 실기 검증 (실측 확정)
7. **조향 부호: 패킷 +steer = 좌회전(CCW)** — Autoware 규약·자체 mock과 동일, 반전 불필요 (예제의 -steer는 키매핑 문제). steer=+0.2→Δh+0.478rad / −0.2→−0.923rad
8. TCP 9910 실기: 1109B @20.2Hz 정상 수신, objects·신호등 필드 확인
9. **9910은 동시 접속 1개만** — 새 연결이 기존 연결을 밀어냄. 주행 중 디버그 툴 병행 접속 금지
10. 리스폰 후 경로 재정렬·연결 유실 후 재접속 로직 실전 작동 확인

### Autoware 준비 (제어기 PC)
11. ROS 2 Jazzy 기설치 확인, **Autoware 1.9.0**(Jazzy 공식 지원) 클론 + 소스 32개 저장소 임포트(~850MB) + 메시지 패키지 빌드
12. **vtd_autoware_bridge ROS 2 패키지 신규 작성·빌드·실기 스모크 통과** — `~/2026-HL-FMA-VTD/ros2_ws/src/vtd_autoware_bridge/`. RX: ego→kinematic_state(20Hz)/TF/velocity·steering·gear·mode report, objects→PredictedObjects(크기 휴리스틱 분류+등속 예측경로), 신호등→TrafficLightGroupArray. TX: control_cmd+turn_indicators→9B. 재연결 처리
13. Lanelet2 맵(33MB)+`map_projector_info.yaml`(projector_type: local) 제어기 PC 배치 (`~/2026-HL-FMA-VTD/map/`)

### 자체 스택 도구 (`controller/` + 제어기 PC `~/2026-HL-FMA-VTD/tools/`)
14. `scp_ctrl.py` — VTD SCP(48179) 원격 제어 (load/init/start/stop/listen, wait_for)
15. `extract_scenario_route.py` — 시나리오 XML Path→경로 CSV (`real_route_path1.csv` = LivingLab_real Path1, 4.9km 순환)
16. `run_real.py` — 실기 러너: SCP 재시작(InitDone→IG ready 대기→Start 1회)/attach 모드/재접속/리스폰 재정렬/통계·플롯
17. `test_steer_sign.py` — 조향 부호 실측 도구
18. **build_route 순환 경로 트림 버그 수정** (lane_graph.py — 시작≈끝이면 2m로 잘리던 것. route_example 822m 회귀 통과)

## 2. 오늘 알게 된 사실 / 판단

- **VTD 헤드리스 운용법 확립**: `sim_start.sh --setup=00_HL_VTD --autoConfig` 필수. Current 셋업 링크가 Standard면 HLVTD 플러그인이 안 실려 9910이 안 열림 (8/25에 링크가 바뀌어 있었음 — 오늘 중단점의 원인)
- **SCP 시나리오 교체는 깨지기 쉬움**: Init 완료 전 Start 반복 전송 → 상태 머신 꼬임/VTD 사망. InitDone+IG ready 대기 후 Start 1회가 안전. 대회장에서는 우리가 SCP 쓸 일 없음(운영측이 조작) — 연구실 자동화 전용 이슈
- 라이선스 서버(lmgrd)는 재부팅/재설치 후 수동 기동 필요(sudo) — 오늘 두 번 발목. `~/HLFMA/run.sh license`
- 교차로 신호는 '신호 제어기' 단위: GT의 trafficLightId는 Ego 진행방향 담당 제어기의 내부 번호. **ID 역추적 금지, state만**. 정지선 위치는 xodr에서 자체 계산
- **충돌은 과실 무관 무조건 우리 감점(중대 –6)** → 교차로에서 신호 무시 차량이 와도 TTC 기반 정지 대응 필요 (회피 조향은 차선 감점 위험, 기본은 정지)
- 사전테스트는 9/3(목) 유지가 유리 (금요일은 혼잡해 자체 시나리오 테스트 어려움)

## 1B. 밤 추가 작업 (23시~)

- **네트워크: 연구실 = 대회장 IP 상시화 완료** (§1-6 참조). 커밋도 완료 (사용자 명의, Claude 흔적 없음)
- **Autoware 트림 빌드 시작** — 인지NN·센싱·측위 제외, 플래닝·컨트롤·맵·API·모듈 전체 (~300pkg). 로그 `~/autoware/full-build.log`, 완료 마커 BUILD-ALL-DONE. 완료 시 브리지 자동 재빌드 체인 걸림
- **브리지 신호등 재설계 구현** (`tl_router.py`) — LaneletRoute 구독 → 경로상 다음 신호등 그룹에 state 적용 (Q&A 반영). **미검증**
- **원커맨드 기동**: `~/2026-HL-FMA-VTD/start_autoware.sh` (실기 연동) / `start_autoware.sh psim` (VTD 없이 플래닝 확인)
- **차량 제원 아이오닉6 반영** (vehicle_info 2곳: wheel_base 2.944, max_steer 0.48 등) + **max_vel 4.17→13.3m/s**
- **전체 검토 문서**: `autoware_통합_검토_0901.md` — 아키텍처·빌드 범위·브리지 코드 검토·규정 15항목 매핑·리스크·**내일 테스트 절차(§7)**
- 관전 카메라: 시나리오 시작 시 ego 후방 10m 부감 자동 설정 (run_real·set_view_camera 'high' 프리셋)

**내일 아침 = 검토 문서 §7 절차 그대로**: 빌드 확인 → `./start_autoware.sh psim`(맵·플래닝) → VTD 켜고 `./start_autoware.sh`(엔드투엔드: 측위→인지→경로→engage→제어)

## 3. 남은 것 / 다음 할 일

### 즉시 (사용자 sudo 필요)
- [ ] **Autoware 의존성 설치** — 제어기 PC에서:
  ```
  cd ~/autoware && rosdep update
  sudo rosdep init   # 이미 돼 있으면 에러 무시
  rosdep install -y --from-paths src --ignore-src --rosdistro jazzy
  ```
  → 완료되면 전체 colcon 빌드(1.5~3h, 백그라운드) → planning_simulator에 LivingLab 맵 로드 = "오토웨어 돌려보기"
- [ ] 제어기 PC git 저장소(`~/2026-HL-FMA-VTD/`)에 오늘 산출물 커밋 (브리지·tools·map·docs 전부 미커밋)

### 실기 (다음 VTD 기동 시)
- [ ] **스폰부터 깨끗한 완주 1판** — `python3 run_real.py` (기본값으로 재시작 포함 실행). 오늘 attach 주행은 무제어 드리프트 665m 후 인계라 오차 통계 무의미
- [ ] 신호등 state 실측 로깅 (교차로 접근 시 어느 시점·어떤 값이 오는지)
- [ ] 장애물·보행자가 objects에 실리는지 (v6/v7 시나리오) → LiDAR 필요 여부 결정
- [ ] 새 IP(50.x)에서 9910 연동 자연 검증 / UDP 9912 LiDAR가 50.10으로 오는지 확인

### 코드
- [ ] **브리지 신호등 재설계** — xodr_signal_id 매핑 폐기, "경로상 다음 신호등 그룹에 state 적용" (Autoware 경로 구독 or ego pose+차선 기반)
- [ ] 자체 스택: 신호등 대응(정지선 s + state), 교차 차량 TTC 가드, 선행차 ACC, 방향지시등(차로변경 3초 내), 스쿨존(붉은 노면 좌표 확보 후)
- [ ] 익스포터 잔여: 횡단보도 출력 / lane_change s-분할 / speed_limit 분리

### 대회 당일 20분 대비 (9/2 00시 추가 — 전원 OFF 제출 → 콜드부팅부터 20분)
- [ ] **자동 로그인 설정** (제어기 PC — 부팅→바탕화면 무조작)
- [ ] **경로 주입 스크립트 작성** (본선까지, 핵심 미구현): `route_config.yaml`(csv_path 한 줄)을 읽어 Autoware에 경로 주입하는 **단독 스크립트**. (9/2 사용자 결정) 원커맨드 통합 안 함 — 대회장은 Claude 없이 사용자가 직접 수정해야 하므로 "설정 파일 + 독립 실행 단계(검증→기동→주입)" 구조 유지, 로직 숨기지 않기
- [ ] 사전테스트(9/3)에서 실측 3건: ①경로 CSV 전달 매체(USB?) ②콜드부팅→주행 시작 소요시간 ③평가 시스템 거동(리스폰·채점)
- 참고: 사전테스트는 자체 스택(`run_real.py <csv>` — 이미 CSV 원커맨드)으로 나감

### 빌드 현황 (9/2 01시 — 미완, 내일 이어서. 서두르지 않기로 함)
- 1단계 노드 폐쇄 171개 ✅ / launch 패키지 단계에서 특수 의존성 가진 미사용 패키지를 하나씩 걸러내는 중 (path_optimizer→acados, tensorrt_common, diffusion_planner 순로 ignore). system_monitor까지 ignore 처리(미사용, launch_system:=false) 후 **5차 체인을 밤새 무인 가동** — 아침에 `cat /tmp/build_watch.result` 확인: ALL-READY면 §7 테스트로 직행, 실패면 같은 패턴(Failed 확인→ignore→`~/finish_chain.sh`)으로 이어가기
- 실패해 있으면: `grep -E "^Failed" ~/autoware/launch-build.log`로 다음 범인 확인 → 해당 패키지 디렉토리에 COLCON_IGNORE → `~/finish_chain.sh` 재실행 (빌드 캐시로 회당 몇 분)
- **acados 정식 해결 계획**: path_sampler 전환은 임시. 연동 확인 후 acados 로컬 빌드(sudo 불필요) → autoware_path_optimizer의 COLCON_IGNORE 제거·빌드 → default_preset.yaml motion_path_planner_type을 path_optimizer로 원복. 본격 튜닝은 기본 구성으로

### 빌드 이슈 기록 (9/2 00시)
- launch 패키지가 exec_depend로 인지 NN·CUDA 체인을 끌고 옴 → **perception/sensing/localization/evaluator 미사용 85개 패키지에 COLCON_IGNORE** 처리로 해결 (플래닝 폐쇄 171개는 이 디렉토리들 의존 0 — 분리 검증됨). 원복: `find ~/autoware/src -name COLCON_IGNORE -delete`
- is_simulation:=true 필수 발견 (신호등 무데이터=통과 처리, 검토 문서 §5B)

### 대기·확인
- [ ] Redmine 미답변 5건 (문서 상단 참조) — 답변 오면 대회정보.md §6·§7 반영
- [ ] 사전테스트(9/3) 지참: 제어기 PC 본체만. 20분 리허설 실측
- [ ] LiDAR ~7fps 원인 (LiDAR 안 쓰면 무시)

## 5. 기술부채 — 빌드 편의로 대충 넘어간 것 전부 (원복법 포함)

> 백업: 제어기 PC `~/2026-HL-FMA-VTD/patches/` (업스트림 수정 patch 2개 + COLCON_IGNORE 목록 88개)

### A. 반드시 정식화할 것 (대회 전)

1. **path_optimizer → path_sampler 프리셋 전환** (acados 미설치 회피)
   - 위험: Autoware 기본·최다 검증 모션 플래너에서 이탈
   - 정식화: acados 로컬 빌드(sudo 불필요) → `autoware_path_optimizer/COLCON_IGNORE` 삭제 → 빌드 → `default_preset.yaml` motion_path_planner_type을 path_optimizer로 원복
2. **~/autoware 소스 직접 수정이 버전관리 밖** — vehicle_info 2곳(아이오닉6), common.param.yaml(max_vel 13.3), default_preset.yaml
   - 조치(오늘 밤): 수정분을 patch로 추출해 repo `patches/`에 저장 ✅
   - 정식화: Autoware 업데이트/재클론 시 `git apply patches/*.patch`로 재적용. 장기적으로는 hlfma_vehicle_description 패키지 분리
3. **COLCON_IGNORE 일괄 88개** (perception/sensing/localization/evaluator 85 + tensorrt_common·diffusion_planner·path_optimizer)
   - 근거는 있음(플래닝 폐쇄가 해당 디렉토리 의존 0) 그러나 개별 검증 없이 일괄 처리
   - 목록: `patches/colcon_ignore_list.txt` ✅ / 원복: `find ~/autoware/src -name COLCON_IGNORE -delete`
4. **launch_system:=false** — engage(자율 전환)가 시스템 진단 없이 되는지 미검증. 안 되면 true로 켜고 diag 구성 조정
5. **tier4_perception/sensing/localization_launch 미빌드** — 우리 launch 인자로는 include 안 되지만, psim 모드가 참조하면 에러 가능. 발생 시 해당 launch 패키지만 증분 빌드

## B. 검증만 하면 되는 것

6. **is_simulation:=true** — 신호등 무데이터=통과 근거는 확인했으나(검토 문서 §5B), 이 플래그를 읽는 **다른** 노드 전수조사는 안 함. 내일 실주행에서 이상 거동 시 의심 목록 1순위
7. **더미 pointcloud_map.pcd (점 1개)** — pointcloud_map_loader 달래기용. 로더가 경고 이상을 내면 이 지점
8. **stderr 있었지만 통과한 패키지들** (eagleye, path_smoother 등) — 경고 수준으로 판단하고 방치. 링크·런타임 문제 시 재확인
9. **실패 패키지의 install 잔재 오염 가능성** — tensorrt_common처럼 실패했는데 install 디렉토리가 남아 "빌드됨"으로 오인된 사례 1건 있었음. 내일 빌드 성공 후 `colcon list --packages-select $(ls ~/autoware/install)` 대조 권장

## C. 브리지·도구의 근사 (기능엔 영향, 편법은 아님 — 관찰 대상)

10. **steering_report = 마지막 명령값** (실측 조향각 아님) — MPC 진동 시 1순위 의심
11. **ego 속도·가속 = pose 미분 추정** (GT에 속도 없음 — Q&A 답변 대기 중)
12. **TlRouter 전체 미검증** (경로 기반 신호등 — 설계만 완료)
13. run_real의 IG ready 대기를 soft-warning으로 완화 (미수신이어도 Start 시도)

## D. 환경 스킵

14. **ansible setup-dev-env 통째로 건너뜀** — acados(→A1), ccache 미설치(빌드 느림만), geographiclib 데이터셋 미설치 가능(local projector라 무관 추정). 문제가 연쇄되면 `./setup-dev-env.sh --no-nvidia --ros-distro jazzy`를 정식 실행하는 게 근본 해결
15. sudoers/자동화 우회들 — pkill 자기매칭, ssh 세션 종료 시 감시자 사망 등 오늘 밤 삽질 원인은 전부 "임시 원격 자동화"였음. 정식화: 재부팅 후에도 유지돼야 하는 것은 systemd 유닛으로 (라이선스 서버 포함)

## 4. 다음 세션 시작 절차

1. `대회정보.md` → 이 문서 읽기 → **Redmine 답변 확인 요청** (상단 ⚠)
2. 장비: `ping 192.168.1.55`(제어기) / `ssh a@192.168.50.11`(시뮬, 제어기 경유)
3. rosdep 완료됐으면: colcon 전체 빌드 걸기 → 빌드 중 실기 검증(§3 실기)
4. VTD 기동: 시뮬 PC에서 `sim_start` 알리아스 또는 `~/HLFMA/sim_start.sh --setup=00_HL_VTD --autoConfig` (라이선스 먼저: `~/HLFMA/run.sh license`)
