# todo 0903 — 세션 핸드오프

## 이번 세션에서 한 것 (2026-09-02 밤 ~ 09-03 새벽)
route 주입~SET 병목을 순차적으로 규명·해결. **route 계층은 뚫렸으나 차는 아직 실제로 안 움직임(제어/구동 계층 블로커 잔존).**

1. **route hang 근본 해결** — `getMainLanelets` 무한루프. gdb 로 멈춘 스레드 스택 확정:
   `getMainLanelets → createMapSegments → DefaultPlanner::plan`, 원인 = 맵의 좌/우 평행차선 위상 순환(예: lanelet 86576)에서 바깥 while 이 무한 append. **가드 추가**(patches/route_handler_getMainLanelets_guard.patch) → route 발행됨.
2. **out of route 해결** — route 가 ego 를 안 지남. 원인 = route_node DP 가 시작점을 ego 실제 차선(56906) 아닌 옆 차선(57203)으로 갈아탐(76m 절감 위해). **시작 앵커를 ego 실제 lanelet 에 고정**(route_node.py build_request) → out-of-route=0.
3. **route 내 순환(73364) 규명** — CSV1(sparse 3점)이 본선 밖 lanelet 75413 에 매칭돼 out-and-back. 임시로 **loop-guard 폴백**(순환 시 set_route_points) 넣었으나, set_route_points 는 ego 시작 안 되는 문제 재발 → **근본은 loopless 웨이포인트 매칭(B1, 미완)**.
4. **acados 근본 해결** — path_optimizer MPC 가 런타임에 acados(/opt/acados/lib) 를 못 찾던 것. **ldconfig 시스템 등록**(/etc/ld.so.conf.d/acados.conf) + start_autonomous.sh 의 LD_LIBRARY_PATH env 줄 제거.
5. 진단 도구: 오프라인 맵/경로 감사 스크립트, 경로 시각화(route_loop.png).

## 다음 세션에서 할 것 — Phase A (차를 처음 움직이게) 우선
- **A1 브리지 IMU**: VTD ego 운동에서 `/sensing/imu/imu_data` 합성 → AEB "waiting for imu" 해소
- **A2 gear 명령**: `vehicle_cmd_gate: GearCommand not received` — /control/command/gear_cmd 흐름 확인/해결
- **A3 궤적 신뢰성**: ldconfig 후 `/planning/scenario_planning/lane_driving/trajectory` 안정 발행 재특성화 (behavior/path 는 10Hz 정상, motion→traj 간헐/부재)
- **A4 engage 안정화**: 위 해결 후 autonomous 유지 + ego 실제 이동 확인
- Phase B: route_node loopless 웨이포인트 매칭(교차로 짝, 좌/우회전 차선) — 대회장 CSV 강건성
- Phase C: 실주행 항목 검증 / Phase D: diag STALE·로그 QUIET·20분 리허설

## 기술부채
- getMainLanelets 가드 = patches/*.patch (클린 재import 후 재적용 필수)
- loop-guard 폴백은 임시 — B1 완성 시 제거/대체
- diag `/autoware/localization/state STALE`, `/routing/state STALE` — 끈 모듈 감시라 sim 에서 필수 제외 검토 필요
- clean_test.csv = 파이프라인 검증용 임시(ego + 전방 185m 직진), 실경로 아님

## 관측 사실 (재현 근거)
- ego 스폰: (842.89,-591.10), yaw 38.1°, lanelet 56906 (Path7 시작=CSV0=ego 일치)
- ego external control: VTD 는 ego 안 몲, 우리 CtrlPacket 필요 → 예전 "잘 감"은 시나리오 route 가 ego 를 끈 것(자율주행 아님)
- behavior/path 10Hz OK, lane_driving/trajectory 간헐(8.6Hz 1회)~부재, mode 계속 STOP(1), gear DRIVE(2)

---

## 09-03 새벽 심층분석 (외부+내부 전수, 아키텍처 관점)

### 방침 (잠정 — 실주행 보며 조정, 확정 아님)
**"충실도(fidelity)는 타협하되, 무결성(integrity)은 타협하지 않는다."**
- 충실도 타협 OK: VTD GT/합성값 사용(정보 원천 제한). 인지는 **GT objects**로 감(대회가 objects를 GT로 줌 → 채점항목 커버, 실 LiDAR 인지는 대공사·불안정). IMU는 **합성**(VTD가 IMU 미제공).
- 무결성 유지: production 진단그래프 삭제로 autonomous 억지로 켜기 금지(은폐), route hang/loop 취약성 금지, 결정론적 실패 재시도 금지.
- 건강-충분 기준: **수정 안 한 production Autoware에 GT/합성값을 충실히 먹여 표준 안전감시 켠 채 주행.**

### 외부 분석 결론 (전부 정상 — 블로커 아님)
- **VTD 인터페이스**: ego GT pose(egoX/Y/Z·heading/pitch/roll) + objects[30] + trafficLights + LiDAR(월드) + 카메라. **IMU·속도·가속·yaw rate 없음.**
- **맵**: `lanelet2_validate` 에러 0·라우팅그래프 유효·위상병리 없음. MetaInfo/format_version 부재는 무해 경고.
- **시뮬 시나리오**(`HL_FMA_VTD_LivingLab_real.xml`): ego = external control + PosAbsolute(route 없음) + (842.89,-591.10)·38.1° → 대회장 방식 정합.

### 내부 분석 결론 (핵심 구조)
- start_autonomous는 `autoware.launch.xml` 직접 기동(perception/localization/sensing/vehicle_interface off) + **production 진단그래프**. 정품 psim의 `tier4_simulator_component`(simple_planning_simulator+pose_initializer+dummy_perception)를 브리지가 대체.
- 브리지가 채워야 할 것들은 대부분 이미 채움: initialization_state=3(수신 확인)·control_mode=AUTONOMOUS·objects·tf·velocity 등.
- **localization/state STALE는 startup 과도기였고 블로커 아님**(브리지가 발행, /adapi/node/localization 수신 확인).

### 이번 세션 수정 (커밋됨)
- **acados ldconfig**(궤적 10Hz 안정화), route hang 가드, route_node 시작앵커·loop-guard (이전 커밋)
- **브리지 /sensing/imu/imu_data 합성 발행**(207bc28) → AEB "waiting for imu" 해소 → **control_cmd 31Hz·CtrlPacket(raw_tx) 20Hz VTD로 송신 시작.** 제어체인이 VTD까지 완결됨.

### ★ 유일하게 남은 블로커 (다음 세션 시작점)
**최종 `/planning/scenario_planning/trajectory`가 안 흐름** → 컨트롤러가 따라갈 궤적 없어 **정지명령(accel −1.5, vel 0) 송신 → 차 안 움직임(velocity 0).**
- 원인: scenario_selector가 lane_driving/trajectory를 **"trajectory is delayed: delay≈5.8s > 1.0s"로 거부**(node.cpp publishTrajectory). current_scenario_=LaneDriving은 정상.
- **핵심 단서: 지연이 거의 일정한 ~5.8초**(5.777/5.810/5.827/5.778). 랜덤 지터 아님 → 궤적이 특정 시점 고정 스탬프이거나 fallback 캐시 궤적 재발행 의심.
- **모순**: vstamp에선 lane_driving/trajectory 지연=0.107s(신선)였는데 다른 run에선 5.8s. **intermittent/state-dependent.** worn 스택 one-shot으론 못 가림 → **다음 세션에 fresh 스택 + rviz/연속관찰**로: behavior_path_planner·motion_velocity 출력 스탬프 소스 확인(now() vs route/cached), "previous trajectory reset" 재발행 여부.

### 다음 세션 순서
1. 위 5.8초 궤적지연 원인 확정(스탬프 소스) → 최종 궤적 흐르게 → **차 첫 이동 확인**
2. route_node loopless 매칭(대회장 CSV) / 실주행 채점항목 관찰·조정
3. diag는 소스충족 유지(그래프 trim 최후수단)

### 기술부채 (누적)
- getMainLanelets 가드 = patches/*.patch (클린 재import 후 재적용)
- loop-guard 폴백은 임시(B1 완성 시 대체)
- IMU는 합성(공분산·값 실주행 검증 필요), 인지 GT objects(미지장애물 회피 없음)
- clean_test.csv = 검증용 임시(실경로 아님)

---

## 09-03 낮~저녁: 첫 자율주행 성공 + 검증

### ★ 첫 end-to-end 자율주행 성공
IMU 합성(마지막 열쇠) 후: engage→AUTONOMOUS 유지 → `/planning/trajectory`(진짜 최종) 추종 → CtrlPacket → **VTD ego 실제 주행.**
- clean_test(직진 185m), short_test(본선 390m 완주), turn_test(회전 388m) 모두 goal 도착·정지 확인.

### 검증 완료 (실주행)
- ✅ **직진·본선 완주 + goal 정지**
- ✅ **회전**: turn_test에서 ego 경로가 좌회전(heading ~57°→~108°, 약 51°), goal 도착. (steer +값, 좌회전 규약 일치)
- ✅ **순항속도**: peak **11.24 m/s ≈ 40 km/h** 도달(상한 46 근접) — 설정 max_vel 12.8(46km/h) 작동. (맵 speed_limit 50 2368개/28 112개)
- ✅ **객체 인지**: turn 주행 중 NPC 8개 검출(브리지 GT objects). 능동 회피 거동은 별도 관찰 필요.
- ❌ **신호등**: 검증한 경로들에 정지선 0개 → 미검증. **신호등 교차로 지나는 경로 생성 필요**(맵 신호등 lanelet 353).

### route_node loopless 수정 (완료)
- 기존: 웨이포인트별 최단경로 독립 이어붙임 → 순환(73364 중복) → set_route_points 폴백(ego 밖 시작 실패).
- 수정: `_expand_sequence`를 **node-exclusion Dijkstra(following+lane change, 이미 지난 lanelet 재진입 금지)** 로 → loopless 보장. 실패 시 **중간 웨이포인트 대안 후보 재매칭**. (커밋 예정)
- 결과: turn_test·short_test에서 out-of-route=0, 폴백 없이 주행. 실제 Path7(3점)은 ego 시작위치 이슈로 별도 확인 필요.

### 알게 된 것 / 오해
- **최종 궤적 = `/planning/trajectory`**(planning_validator 발행). `scenario_planning/trajectory`는 미사용. (오래 헤맨 오해 — 노드그래프 문서화로 재발방지)
- **ego 위치 누적**: VTD external control이라 스택 끄면 ego가 멈춘 자리에 남음 → 다음 테스트가 거기서 시작. 깨끗한 시작점 테스트하려면 시나리오 재로드 필요(SCP 리셋은 리스크로 보류).
- **record.sh**: 0바이트 bag은 인라인 setsid 종료 방식 탓(record.sh의 exec+Ctrl+C면 정상). `-a` 부하 큼(occupancy·pointcloud·마커) → **경량화**(--exclude, `ALL=1`로 전체) 적용.

### 네트워크 (대회장 대비)
- 라우팅 정상: 192.168.50.0/24→enp89s0(이더넷), wifi 192.168.1.x 충돌 없음.
- **DDS loopback 격리 미적용** → 이더넷/wifi/tailscale로 DDS 누출. `ROS_LOCALHOST_ONLY=1` 권장(SSH·VTD raw TCP 무관). **대회 전 적용·검증.**
- chrony 불필요(전부 제어기 clock 스탬프), MTU/점보 불필요(LiDAR 미사용, 단 나중 쓰면 필요).

### 다음 세션 우선순위
1. **신호등 경로 생성·검증**(신호등 교차로 지나는 route)
2. **속도추정 노이즈** 개선(방향은 GT라 정확 → 헤딩 방향 투영으로 속도 정제 여지)
3. 실제 Path7 시작점부터(시나리오 재로드 후) 회전·교차로·신호등 종합
4. DDS loopback 적용, 20분 부팅→연동 리허설
5. 능동 객체회피·보행자·NPC돌발 거동 관찰

### 기술부채 추가
- route_node loopless는 node-exclusion이라 어중간한 ego 위치에선 대우회 가능(112 lanelet 사례) — 시작점 정상이면 무난. 대회장 짝 CSV로 재확인.
- 합성 IMU·속도추정 공분산/필터 실측 검증 미완.
