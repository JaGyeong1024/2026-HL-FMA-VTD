# todo 0906 — 사전테스트 세팅 대비 변경 감사 (세션 핸드오프)

## 이번 세션에서 한 것 (2026-09-06 저녁)
제어기 PC(Tailscale)·시뮬 PC(제어기 경유)에 SSH 접속해 **9/3 사전테스트 시점 세팅과 현재의 차이**를 전수 감사. 코드 변경 없음(읽기만). 노트북 로컬 문서가 9/3 에 멈춰 있어 제어기의 `todo0903.md`(9/3 14:59 판)·`수정내역_0906.md`·`문서_인덱스.md`·`문서보관/검토보고_0905_정체차로_교착.md` 를 로컬로 가져옴.

### 기준점
- 사전테스트 = `test/20260903_1437xx ~ 145424` 녹화. meta: git `b5e2597` + dirty 8 (그 dirty 는 9/4 `27fd15e` 로 커밋된 record.sh·start_autonomous.sh·문서. 스크립트 mtime 9/3 14:11·14:45 → 사전테스트 당시 내용 그대로). **실질 기준 = `27fd15e` 의 코드·설정.**
- 현재 = `develop-ng 8b2208f`(9/5, origin 미푸시) + 미커밋 변경(9/6 17:49~18:06 계속 수정 중 — 제어기에서 다른 Claude Code 세션 활동 중).

### A. 제어기 — 코드·설정 차이 (전부 플래닝 계층. 통신·브리지 본체·route_node·맵·차량제원·기동 스크립트는 동일)
| # | 파일 | 사전테스트 | 현재 | 검증 |
|---|---|---|---|---|
| 1 | `default_preset.yaml` | static_obstacle_avoidance / avoidance_by_lane_change / run_out / external_request_LC L·R 전부 false | 5개 전부 **true** | 미검증(run_out 만 9/4 부분) |
| 2 | `static_obstacle_avoidance.param.yaml` | opposite_direction_lane, intersection/hatched/freespace true, ignore_area 20m | same_direction_lane, 셋 다 false, ignore_area **5m**(미커밋) | 미검증 |
| 3 | `common.param.yaml` | min_acc −2.5 | **−4.0** | 계산만 |
| 4 | `velocity_smoother.param.yaml` | engage 0.25 / 0.5 / exit 0.5 | 0.5 / 1.0 / **0.1** | 실주행 확인 |
| 5 | `scene_module_manager.param.yaml` | enable_rtc 전부 false | external_request L·R **true** + **일반 lane_change_left/right 도 true**(미커밋, 18:02 수정) | 미검증 |
| 6 | `bridge.launch.xml`·setup.py·package.xml | — | **blocked_route_detour 노드 상시 기동** 추가(209줄, 18:06 에도 편집 중), 의존 2개 추가 | 미검증 |
| 7 | 업스트림 패치 | route_handler 가드 1건(사전테스트에도 적용) | + `external_request_lane_change_non_preferred.patch`(9/6 17:34 적용, 17:36 빌드 → **현재 install 에 반영됨**) | 회귀테스트만 |

**★ #5 문서–트리 불일치**: `수정내역_0906.md` §5-2·§6 은 "일반 lane_change 는 enable_rtc:false(자동승인) 유지가 의도" 라고 명시했는데, 실제 파일은 18:02 에 일반 LC 까지 true 로 바뀜. 결과: **경로상 필수 차선변경도 blocked_route_detour 의 ACTIVATE 없이는 실행 안 됨.** 노드 다운·입력 stale(1초)·corridor 조건(finish_distance+5m) 미충족이면 차선변경 0 → 경로이탈 리스폰. 사전테스트엔 없던 단일 실패점.

**로그 상 신규 에러** (사전테스트 autoware.log 0건 → 9/6 17:51 run 발생): `map_hash_generator process has died (exit 1)`, `mrm_handler: EMERGENCY_STOP call timed out` ×2. 원인 미확인.

**저장 상태**: `8b2208f` origin 에 없음. 미커밋 9파일 + 신규 4개. `hlfma_ws/src/autoware_0906남규.zip` 494MB untracked(9/1~9/2자 업스트림 사본, 압축 해제 안 됨, `src/autoware` 와 무관) — `.gitignore` 에 안 걸림, 실수 커밋 주의.

### B. 제어기 — 환경 (동일 / 미해결 그대로)
- 네트워크 동일: enp89s0 `hlfma-venue` 192.168.50.10/24 (autoconnect 우선 100, never-default), wifi 192.168.1.55, tailscale. FORWARD enp89s0↔tailscale0 규칙·dispatcher 유지. 시뮬 ping 0.14ms.
- `ROS_DOMAIN_ID=43` 동일. **`ROS_LOCALHOST_ONLY` 여전히 미적용**(todo0903 과제 그대로).
- `.bashrc` 9/6 17:11 변경 = npm-global PATH 한 줄 추가뿐. acados ldconfig 유지. 맵 `map/lanelet2_map.osm` md5 = 로컬 `livinglab_lanelet2_v2.osm`(파일명은 native 이지만 내용은 v2). 차량제원 Ioniq6 동일. `route_config.yaml` = route_pretest.csv.

### C. 시뮬 PC (연구실 VTD) — 재현 충실도
1. **★ ego 차종 불일치**: 9/4 `_1.xml`, 9/5 `_2.xml`(현재 로드 중) 의 Ego = **`AlfaRomeo_Brera_10_BiancoSpino`**(ScenarioEditor 기본값). 대회·`_real`·ORIG = `HyundaiIoniq6_23_Black`. `_1` 생성 22:04 → 9/4 avoid_verify 22:12 이므로 **9/4 이후 검증(run_out·min_acc −4.0·engage 재출발)은 Alfa 로 수행된 것으로 판단.**
   | | Ioniq6 | Alfa Brera |
   |---|---|---|
   | WheelBase | 2.944 | 2.536 |
   | DistFront(범퍼) | 3.808 | 3.536 |
   | Mass | 2095 | 1705 |
   | MaxSteering | 0.48 | 0.46 |
   | TurningCircle | 12.53 | 10.79 |
   Autoware `vehicle_info` 는 Ioniq6 → 모델 불일치 상태에서 튜닝됨. 정지선 범퍼 판정 27cm, 제동거리·조향 응답 모두 어긋남.
2. `_1`/`_2` 는 NPC 거의 없음(`_2`: Ego+보행자 1, Path 1개) → 사전테스트 실패 3건(갓길 정차차·급출현·사고 우회)의 로컬 재현본 없음. 회피 튜닝 검증 환경 자체가 없음.
3. 동일·정상: `hl_vtd_config.json` 9/1 이후 무변경(9910/48190, lidarDest 192.168.50.10, Ioniq 제원, 리스폰 규칙). `Setups/Current→00_HL_VTD`, `Projects/Current→SampleProject`(9/6 16:52 재링크, 대상 동일), vpj 기본 시나리오 `_real`. lmgrd+MSC 가동, nvidia 580.173, 9910/48179/48190/8554 리슨.
4. 네트워크: 실제 IP 는 NM 프로필 `control`(192.168.50.11, gw 192.168.50.10). 대회정보.md §9 "netplan 고정 IP" 는 구설명(netplan 파일엔 192.168.10.2 잔존, 미적용). `control` 에 192.168.10.1 default route 잔존(무해, 정리 대상).

## 다음 세션에서 할 것 (우선순위)
1. 시뮬 시나리오 `_1`/`_2` Ego → `HyundaiIoniq6_23_Black` 교체 후 9/4~9/6 튜닝(run_out·min_acc·engage) 재검증. 가능하면 갓길 정차차·급출현 NPC 재현 시나리오 작성(ScenarioEditor).
2. `scene_module_manager` 일반 `lane_change_left/right enable_rtc` — 문서대로 false 원복, 또는 true 유지 시 근거 문서화 + detour 노드 다운·stale 시 필수 차선변경 폴백 실주행 검증.
3. 커밋·푸시(`.gitignore` 에 `*.zip` 추가 후). develop-ng 를 origin 에.
4. 신규 에러 2건(map_hash_generator died, EMERGENCY_STOP timeout) 원인.
5. `ROS_LOCALHOST_ONLY=1` 적용·검증, 20분 리허설.
6. 대회정보.md §9 시뮬 PC 네트워크 설명 현행화(NM `control` 프로필).

## 기술부채 (이번 감사로 추가)
- 9/4 이후 실주행 검증 = Alfa Romeo ego (원복: 시나리오 Ego Type 을 Ioniq6 로)
- 일반 LC RTC 수동화(원복: enable_rtc false 2줄)
- 494MB zip untracked(원복: 삭제 또는 저장소 밖으로)

---

## 추가 감사 (9/6 18:10~18:21) — 사전테스트 이후 팀원 변경분

**누가**: git 커밋(27fd15e·8b2208f)은 전부 사용자 명의. 미커밋 변경은 **192.168.1.67 에서 SSH 로 접속한 팀원 세션(pts 3개, 16:59~)** 이 지금도 진행 중. 제어기 `.claude/projects/-home-a-2026-HL-FMA-VTD/` 세션 파일도 갱신 중 → Claude Code 로 작업 중.

### 18:06 이후 실제로 바뀐 것
1. **`blocked_route_detour.py` 재작성** (18:06→): 정지 판정을 behavior path 0속도 → `/planning/planning_factors/{traffic_light,intersection,crosswalk,stop_line}` STOP factor 로 교체 / blocker 감지를 ego 헤딩 직선 → behavior path 호길이 투영(곡선 대응) / corridor(check_objects=) 정리. 런치 파라미터 `hold_distance_m 45`, `approach_speed_mps 4.0` 명시(map_osm 인자 제거).
2. **단위테스트 22개** `vtd_autoware_bridge/test/test_blocked_route_detour.py` — pytest 캐시 lastfailed 비어 있음(전부 통과).
3. **업스트림 패치 확장**: `external_request_lane_change_non_preferred.patch`(18:09) 에 `scene.cpp` 훅 추가(external-request 후보 거절 사유를 `DETOUR reject` WARN 으로 출력).
4. **★ 소스–바이너리 불일치**: `src/autoware/universe` 의 utils.cpp·scene.cpp·test 가 **9/1 원본으로 되돌아감**(git status clean, mtime 9/1). 그런데 `libautoware_behavior_path_lane_change_module.so` 는 18:10:34 빌드본으로 **패치 문자열 포함** → 지금 도는 Autoware(18:19 run)는 패치 적용본, 소스는 미적용본. **다음 `colcon build` 에서 패치가 사라짐** → 재적용 필수(`patch -p1 < patches/external_request_lane_change_non_preferred.patch`). 왜 되돌렸는지 불명(백업 zip 후 checkout 한 흔적).
5. zip 2개 = 팀원 수정본이 아니라 **`src/autoware` 스냅샷 백업**: `autoware_0906남규.zip`(16:32, 9/3 01:10 route_handler 패치 시점 상태), `ㅁㅇ.zip`(18:17, scene.cpp 18:09 패치 적용 상태 포함). 트리와 파일명·크기 비교 결과 nebula/muSSP 문서 파일명 인코딩 차이 외 동일. 둘 다 494MB untracked → 저장소 밖으로.
6. **시뮬 `_2.xml` Ego → `HyundaiIoniq6_23_Black` 로 교정됨**(18:18, `~` 백업 = Alfa). 20 players. `_1.xml` 은 여전히 Alfa.
7. 18:19 run(route_pretest_2.csv, rviz 켜고) 진행 중: DETOUR status 에 blocker 13.5m, route_left 후보 safe=False 로 승인 대기. autoware 로그에 `DETOUR reject` 0건.

### 다음 세션 추가 과제
- 팀원 세션 종료 후 `patch -p1` 재적용 여부 확인 → 소스와 .so 일치시킨 뒤 커밋(패치·detour·테스트·yaml 한 묶음)
- `_1.xml` Ego 도 Ioniq6 로

---

## 18:40 브랜치 정리 (사용자 정책: main = 실주행 검증 코드만, 나머지는 이슈별 임시 브랜치)
- 팀원 작업(zip 백업분) → **`develop-ng`** 에 커밋 `01c8fc2`(`8b2208f` 위), origin 푸시 완료(issue 브랜치는 사용자 지시로 삭제). 이 브랜치의 docs/ 에 todo0906·수정내역_0906·검토보고_0905 있음.
- `.gitignore`: 팀원이 주석 처리한 `hlfma_ws/src/autoware/` 무시 규칙 원복, `*.zip` 추가.
- 제어기 작업트리 → **main `27fd15e`** (= 사전테스트 코드). lane_change 모듈 `.so` 를 깨끗한 소스로 강제 재빌드(18:43, 패치 문자열 0). route_handler 가드 패치는 유지(테스트 상태의 일부).
- 팀원이 돌리던 Autoware(18:25 기동)는 안 건드림 — 그 run 은 옛 .so·develop 설정으로 계속 동작 중, 다음 실행부터 main 구성.
- 남은 zip 파일 2개(`ㅁㅇ.zip`, `미커밋_2번시나리오작업_20260906.zip`)는 ignore 되어 있으나 저장소 밖으로 옮길 것.

- 18:45 팀원이 main 작업트리의 `.gitignore` 에서 `hlfma_ws/src/autoware/` 무시 규칙을 다시 주석 처리함(미커밋). 커밋되면 업스트림 34개가 깨진 서브모듈 포인터로 들어감 → 팀원과 조율 필요.

## 19:00 디렉터리·브랜치 재편 (사용자 결정)
- 정책: `~/2026-HL-FMA-VTD` = main(검증 코드)만. 개인 사본 `-JG`·`-MY`·`-NG`(cp -r 로 만든 완전 사본)에서 브랜치 작업 → 검증 후 main 머지.
- **업스트림 소스 편입**: `hlfma_ws/src/autoware` 를 파일로 추적(내부 .git 제거됨, 11,342 파일 624MB, >50MB 없음). main `804adf6` 커밋·푸시 완료. 이후 업스트림 수정은 patches/ 대신 소스 커밋. `hlfma.repos` 는 사실상 폐기(문서 갱신 필요).
- 브랜치 연결: JG→`develop-jg`, MY→`develop-my`(둘 다 main 에서 분기, 푸시됨). NG→`develop-ng` 를 새 main 위로 rebase(7ea31fc, b260f4f) + lane_change 패치를 소스 커밋 `68aeeb5` 로 반영. **origin/develop-ng 는 아직 옛 이력(01c8fc2) → `git push --force-with-lease origin develop-ng` 필요(사용자 실행).**
- ★ **사본의 build/install 은 원본 경로를 가리킴**(symlink-install 심링크·CMakeCache·egg-link 전부 `/home/a/2026-HL-FMA-VTD/...`). 사본에서 편집해도 원본 소스가 실행됨. 각 사본에서 `rm -rf hlfma_ws/build hlfma_ws/install` 후 재빌드 필요: 전체(수 시간) 또는 main install 을 underlay 로 두고 `--packages-select autoware_launch hlfma_vehicle_launch vtd_autoware_bridge (+수정한 업스트림 패키지)` 오버레이(수 분).
- `~/.bashrc` 의 main install source 줄 제거 필요(사본 실행 시 환경 혼입).

## 19:10 개인 사본 초기 세팅 완료 (오버레이 방식)
- 사본 3개(`-JG`·`-MY`·`-NG`): 원본을 가리키던 build/install/log 삭제, 중복 zip 삭제 → main 의 `hlfma_ws/install` 을 underlay 로 두고 `colcon build --symlink-install --packages-select autoware_launch hlfma_vehicle_launch vtd_autoware_bridge` (NG 는 `+ autoware_behavior_path_lane_change_module`, 패치 반영 확인).
- 검증: 각 사본 env 에서 autoware_launch·bridge·preset yaml → 자기 디렉터리, route_handler 등 나머지 → main install. 빌드 9초/9초/1분47초.
- 사용 규칙: 사본에서 `./start_autonomous.sh` 그대로 실행(자기 install → main install 체인). **업스트림 C++ 패키지를 고치면 그 패키지를 `--packages-select` 에 추가해 빌드**해야 오버레이됨(안 하면 main 바이너리가 돎). main 이 재빌드돼도 체인은 경로 기반이라 유지.
- main 디렉터리는 손 안 댐(트리 clean, .so 18:43 그대로).

---

## 9/6 저녁 세션 (노트북) — 저장소 재편 확인 + 수정 타겟 확정 (코드 검증 기반)

### 이번 세션에서 한 것
- 제어기 저장소 재편 확인: 사람별 클론(`~/2026-HL-FMA-VTD`=main 804adf6, `-JG`(develop-jg 71cd80b), `-NG`, `-MY`). 업스트림 autoware 소스 편입, 중첩 .git 0개·gitlink 0개. `hlfma.repos`·`patches/`는 이력용으로만 남음. **main·JG 코드 = 사전테스트(27fd15e) 동일** (차이: JG 71cd80b 자동 engage 스크립트, `.bashrc`가 install 을 source 안 함, route_config = route_pretest_2.csv).
- 시뮬 시나리오 Ego 전부 Ioniq6 로 교정(`_1.xml` 9/6, `_2.xml` 팀원). **결정: 시나리오 Ego 는 항상 HyundaiIoniq6_23_Black**. 백업 `~` 파일 삭제.
- JG 클론 미커밋 변경 2건: `common.param.yaml max_vel 13.6`(49km/h 결정 → 아래 13.3 권고로 재조정 예정), `대회정보.md`(CSV 점 = 근방 통과, 주최측 확인).
- 사전테스트 실패 4건의 근본 원인을 소스로 확정 (스크래치 분석 3건: risk_avoidance.md / risk_motion.md / recovery_process.md — 노트북 세션 스크래치패드, 요지는 아래).

### 확정된 근본 원인 (코드 근거)
| 증상 | 원인 | 근거 |
|---|---|---|
| 정차차 앞 정지·미회피 | 회피 모듈 4종 off. SOA 는 켜도 "최우측 차선 우측 절반 정지차"만 대상(객체 쪽 이웃 lanelet 있으면 제외, 예외 road_shoulder=맵 0개) | SOA utils.cpp ~820-870 |
| 좌회전 차선 정지차(녹색) 미회피 | SOA 가 "다음 신호등 20m 이내 정지차 = 대기중"으로 판정, **신호 색 무시** | SOA utils.cpp:699-731 isCloseToStopFactor |
| 보행자 급출발 미정지 | run_out off + 스무더 `normal.min_acc -1.0`(limit.* 는 스무더가 안 읽음) → 정지거리 99m | velocity_smoother node.cpp:203 |
| 차선변경 미발동 | 필요길이 83m(병목 `lateral_jerk 0.5`) + 개시금지 `max_prepare 4.0×max_vel=54m`(접근로 lanelet 중앙값 47m) | path_shift.cpp:70, lane_change scene.cpp:317 |
| 정지 후 재출발 교착 | engage 0.25/0.5 → 명령 ≈0.42 m/s² (VTD 데드밴드 추정) | smoother calcInitialMotion, PID ff_scale 0.5 |
| 리스폰 후 정지 | route uuid 불변 → behavior 모듈·현재 lanelet 추적 리셋 안 됨 → out of route | planner_manager.cpp:161-172 |

### 수정 타겟 확정 (JG 클론, 커밋 3묶음, 각 묶음 뒤 시나리오 주행)
**묶음 1 — 정지·감속**: common `normal.min_acc/min_jerk` −1.0/−1.0→**−2.5/−2.0**(2차 −3.5/−3.0), `normal/limit.max_acc` 1.0→**1.5/2.0**, `max_vel` **13.3**; preset `run_out` **true**; run_out `slowdown/stop deceleration_limit` → **2.5/4.0**(=normal.min_acc 절대값); velocity_smoother **8b2208f 3값**(engage 0.5/1.0/exit 0.1) + pid `enable_integration_at_low_speed true`.
**묶음 2 — 차선 단위 회피 (9/7 새벽 적대적 검증 후 재확정, 근거 scratch adv_bundle2.md)**
- `avoidance_by_lane_change` **사용 불가 → 제외**: isExecutionRequested 가 isLaneChangeRequired()(optional<에러>) 를 bool 로 써서 의미 반전(abl interface.cpp:48-52) + 우선차선에서 target 없음. 모든 차선 단위 회피는 **external_request LC** 로 통일.
- **업스트림 패치 2건 필수** (설정으로 대체 불가): P0 = NG 68aeeb5(비우선차선에서 external 후보), **P1 = 우선차선에서 external 후보**(calc_shift_intervals 가 우선차선이면 빈 벡터 → 최소 LC 길이 DBL_MAX → 후보 0, calculation.cpp:271-310 / route_handler.cpp:1927-1932. 비필수 타입이고 intervals 비면 현재↔target 중심선 횡거리 1개 반환). NG 18:19 주행에서 external 후보 0건이었던 원인.
- preset: external_request L/R **true**, SOA·abl false. scene_module_manager: **external_request 둘만** enable_rtc true (NG 가 일반 LC 도 true 로 바꾼 것은 원복 — 노드 다운 시 필수 차선변경 불가).
- lane_change `max_prepare_duration 2.0` 필수(묶음 3 선행): 규제요소 근접 금지(26.6m=2.0×13.3, 색 무관, external 에도 동일 적용 scene.cpp:296-328). 4m/s 접근 시 우회 시작+복귀 모두 26.6m 밖이어야 → **정지차~정지선 ≥ 약 35m 일 때 우회+복귀 가능**(통과 8 + 복귀 LC 19 + 정지 여유 3, 복귀 시작이 26.6m 밖). 26.6m 는 max_prepare×**common max_vel** 곱(현재 속도 무관)이라 최선 아님 → **P2 후보**: 규제 근접 검사에 min(현재속도, 외부 속도제한)×max_prepare 사용 → 유지 접근(4m/s) 시 8m, 조건 ≥ 30m(물리 하한). **타이트 설정(9/7 결정 방향)**: P2 즉시 적용 + lateral_jerk 1.5(복귀 LC 17m) + 통과 여유 6m + 정지 여유 2.5m → 계산 하한 **26m**, 판단 노드 임계 **28m**(실패 비대칭: 복귀 실패=경로이탈 리스폰 −6, 대기=시간만 → 2~3m 남김). 유지속도 4m/s·RSS 는 조이지 않음(저속 조향 포화·충돌). 시나리오에서 정지차 위치 25/30/35m 로 경계 실측 후 확정. 미만은 HOLD/STOP 이 옳음(접근로 중앙값 47m).
- **판단 노드**: NG blocked_route_detour 골격(RTC 승인·속도제한·planning_factors 게이트·corridor·테스트 하네스) 재사용, **판단 규칙 교체**: 차량 클래스만 blocker(보행자 제외) / 정지 ≥3s(blocked_time 0.4→3.0) / 대기열 판정 = 우리 방향 신호 비적색 **AND** 객체~정지선 거리 ≥ 28m(파라미터, 타이트 설정 기준)(신호 0·타임아웃 시 factor 부재로 "비적색" 오판 방지) / 취소(ABORTING) 관측 시 back-off·재승인 상한 / hold 4m/s 와 보행자 30km/h 를 **sender 분리**(같은 sender 면 덮어씀, clear 는 sender 별) / 리스폰 시 velocity limit clear + 카운터 리셋 + 1.5s 속도 0 / 우회 후 우측 차선에서는 LEFT 만 재요청 가능. NG 의 route_* 승인 로직은 죽은 코드 → 제거. 테스트: 신호색·대기열·보행자·정지선 거리 케이스 추가(기존 22개엔 0개).
- 실선 판정: 후보가 lane_change≠yes 경계와 교차하면 throw(path.cpp:510-513). 맵 태그가 섹션 중점 근사(no 1808/yes 1077) → 오탐 시 익스포터 lane_change 태그 정밀화가 선결.
- 결과 예측: 갓길 정차차 = P1 후 external LEFT 로 풀림(좌측 흐름 RSS·실선·신호 26.6m 안이면 불가). 좌회전 차선 정지차 = 정지선 ≥28m(타이트 설정) 일 때만.
**묶음 3 — 차선변경**: lane_change `lateral_jerk` 0.5→**1.0**, `lateral_acceleration` 1.2/0.6, `max_prepare_duration` 4.0→**2.0**, `th_prepare_curvature` **0.04**. safety_check(RSS) 는 미변경 — 뒤차 접근 시 대기 후 진입은 현 값으로 정상 동작 확인.
**A13 — 리스폰 재주입(route_node) (9/7 재확정, 근거 adv_a13_scripts.md)**: 트리거 = 브리지 `/vtd/respawn` 만. **`set_route_points` 재호출은 SET 상태에서 무조건 거부(adapi routing.cpp:225-233) → `change_route_points` 사용**(route_node 에 클라이언트 추가). AUTONOMOUS 중 재라우팅은 정지해도 승인 모듈(LC·회피) 잔존 시 거부(mission_planner.cpp:306-310, planner_manager.cpp:366-381) → **절차 = change_to_stop → change_route_points(새 uuid, 모듈 리셋) → change_to_autonomous**(정지 engage 즉시 통과 확인). 응답 코드 검사, 이벤트마다 attempts 리셋, on_respawn 에서 즉시 실행(0.5s spin 대기 금지; 크립 속도 시 안전검사 진입). **지나온 CSV 점 필터 = `/planning/mission_planning/route`(latched) 세그먼트 열에서 자차 세그먼트 인덱스 k 이후 점만(같은 세그먼트는 s 비교)** — full_lanelets 기준은 폴백 시 None·병렬차선 오매칭 결함. 리스폰 2회 연속: REROUTING 중 두 번째는 route_is_not_set 거부 → 응답 기반 재시도. PID enable_large_tracking_error_emergency 는 미참조(죽은 파라미터) → 추적오차 비상 없음.
**부수 결함(수정)**: `bridge.launch.xml:4` map_osm 기본값이 main 디렉터리 맵 → JG 도 main 맵을 읽음. `map_osm:=$ROOT/map/lanelet2_map.osm` 전달.

**기동 스크립트 안전 순서 (사용자 요구 9/6 밤)** — 현 결함: sleep 3 후 VTD 연결·맵 로드 미확인 / route_node 3회×3s 후 영구 포기(콜드부트 맵 지연) / start_hlfma 가 avail false 여도 60s 후 engage 강행 / cleanup·stop.sh pkill 이 경로 무관 → **MY·NG 클론·rviz 까지 죽임** / 세 클론 ROS_DOMAIN_ID 43 공용 → 다른 클론 mission_planner 에 경로 주입 가능 / run_latest.env 공용. 체크리스트: ① 잔존 노드(`pgrep -f hlfma_ws/install` 경로 표시)·`nc -z` VTD 9910·CSV 존재·맵 md5 → 실패 시 중단 ② 브리지 로그 "VTD 연결"·"맵 로드" 15s 폴링 ③ `/api/routing/state`==UNSET 120s ④ route_node 시간 기준 재시도(120s), SET 대기, 거부 시 사유 출력 후 중단 ⑤ is_autonomous_mode_available true 60s, false 면 **중단**+진단 요약 ⑥ engage 3s 후 mode==AUTONOMOUS 확인 ⑦ run_latest.env 에 ROOT·GIT_REV·맵 md5, "READY" 출력, record.sh ROOT 일치 검사. 클론별 DOMAIN_ID 분리(43/44/45) 또는 pkill 을 `$ROOT/hlfma_ws/install/` 로 한정.

### 검증으로만 확정되는 것 (재현 시나리오 Ioniq6, `ALL=1 ./record.sh`)
1. VTD 출발 데드밴드 실측(정지에서 accel 계단) → 묶음1 재출발 값 근거
2. 정지 5m 뒤 swerve(close_vehicle auto) 실현 여부·저속 조향 포화 → 안 되면 NG 외부요청 LC 검토
3. 회피 후 좌회전 차선 복귀가 규제 27m 안에서 되는지(D11)
4. 차선변경 추종 오차 취소(path_miss 1.06m), MPC 합성조향 신뢰
5. RDB 실속도 vs 추정 → max_vel 확정
6. 보행자 GT 크기(<1.2m 분류), run_out 인도 보행자 오탐 빈도
7. 리스폰 재주입 실제 동작, 교차로 내 신호 0 급정지(D5) 관측 시 브리지 신호 기억 구현
8. 교차로 좌회전 중 우측 직진차 양보 — 원인 미확정, intersection 디버그부터

### 설계 원칙 (사용자 지시 누적)
- 맵 의미(신호·정지선·보호구역)는 맵 기반, 회피·재라우팅은 GT 객체 기반. 전역경로는 한 번 계산 후 고정, GT 보정은 behavior 층. 재라우팅은 전 차선 막힘의 최후 수단(정지 상태에서만, clear 없이, 1회).
- 가정 위 설계 재검토 기준: 가정이 틀리면 **설계**가 바뀌는 항목은 구현 전 확정, **값**만 바뀌는 항목은 측정 절차 붙여 진행.
- 보호구역 30km/h(붉은 노면 → 익스포터 speed_limit) 는 아키텍처에서 빠진 감점 항목 — 목록에 추가, 순위는 묶음 3 뒤.

### 다음 세션에서 할 것
1. JG 빌드 완료 확인 → 묶음 1 반영·빌드·커밋 → 데드밴드 실측 + 보행자 시나리오
2. 묶음 2: 업스트림 P0+P1 패치·빌드 → NG 노드 골격 재사용 + 판단 규칙 교체 → 갓길 정차차·좌회전 차선 정지차(정지선 25/30/35m 경계 실측) 시나리오. 기동 스크립트 체크리스트 반영은 0단계에
3. 묶음 3 → 필수 차선변경 시나리오. A13 route_node 수정은 묶음 2와 함께
4. 검증 항목 1~8 결과를 todo 에 기록, 검증된 묶음만 main ff-merge
