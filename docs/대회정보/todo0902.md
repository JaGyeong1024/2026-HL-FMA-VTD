# 2026-09-02 세션 정리 + TODO — Autoware 빌드 완성

다음 세션 이어가기용. 전체 배경은 `대회정보.md` 먼저. **진행 상황·다음 할 일은 이 문서가 최신** (이전: todo0901.md → 문서보관/).

> **결론 먼저**: Autoware 트림 빌드 **완성** (292개 패키지, 브리지 포함). psim에서 **자율주행 E2E 검증 통과** (initialpose→route→engage→주행, 속도 1.63m/s 실측). acados 정식 설치로 path_optimizer(기본 MPC 플래너) **원복 완료** — 임시 회피였던 path_sampler 전환 폐기. **sudo/사용자 개입 필요 항목: 없음.**

**✅ Redmine #4939 5건 답변 완료 (9/2 낮, 캡처 `게시판 QnA/qna_answers_0902_*.png`, 대회정보.md §6·§7·§13 반영)**:
1. 정지선 위치 — 별도 제공 없음, "센서로 식별" → xodr signal 정지선 자체 추출 유지
2. 보호구역 — 붉은 노면, **30km/h**, 좌표/범위 미제공 → xodr에서 붉은 노면 요소 탐색 필요(미확인), 없으면 카메라
3. state 6 — 적·황 점멸 무구분 → 보수적으로 **state 6 = 무조건 일시정지 후 통과**
4. objects[].id — 시나리오 내 유지(삭제·재생성 시 변경 가능) → id 추적 사용 가능
5. egoSpeed — GT API 미포함, **평가는 RDB 실속도** → 속도 마진 확대(제한 –2~3km/h)

---

## 1. 빌드 최종 상태 (9/2 새벽 확정)

| 항목 | 상태 |
|---|---|
| Autoware 워크스페이스 | **296개 패키지 빌드·설치 완료** (Release, symlink-install) |
| 마지막 체인 | 9차 `~/finish_chain.sh` → `/tmp/build_watch.result` = **ALL-READY** |
| 브리지 (`ros2_ws`) | vtd_autoware_bridge 재빌드·임포트·launch 파스 검증 통과 |
| COLCON_IGNORE | **79개** (88→79, 목록 `patches/colcon_ignore_list.txt`와 1:1 대조 검증됨) |
| 모션 플래너 | **path_optimizer (acados MPC) — Autoware 기본 구성 원복** |
| acados | v0.5.3, `~/acados` 로컬 설치 (962MB, sudo 불필요) |

### 검증 내역 (전부 실측)

1. **install 정합성** (기술부채 B9 해소): install/ 292개 전부 워크스페이스 유효 패키지, stale install 0, package.sh 마커 전수 OK
2. **acados 링킹**: `libacados_interface.so` → `~/acados/lib` 해석 확인 (ldd)
3. **psim 헤드리스 스모크**: launch 예외 0, 프로세스 사망 0, vector_map 발행(구독 9), 127개 노드 기동
4. **psim 자율주행 E2E** (`tools/psim_e2e.sh`):
   - initialpose 주입 → localization OK (kinematic_state 발행)
   - ADAPI set_route_points → route SET (lanelet 214, goal_planner pull-over 후보 12개 생성)
   - `AutowareState: Planning => WaitingForEngage`
   - change_to_autonomous → **operation mode 2 (AUTONOMOUS), 차량 주행 속도 1.63m/s 실측**
   - 프로세스 사망 0

## 1B. 저녁 추가 수정 (9/2 저녁, 사용자 psim 확인 중 발견)

1. **start_autoware.sh `set -u` 제거** — ROS setup.bash가 미정의 변수를 참조해 nounset과 비호환 (기동 즉시 실패했었음)
2. **rviz 맵 미표시 원인 = `autoware_lanelet2_map_visualizer` 미빌드** — 맵 데이터(/map/vector_map)는 정상이었고 표시 전용 composable 노드만 빠져 있었음. composable 로드 실패는 launch를 죽이지 않아 헤드리스 검증에 안 걸림
3. **`<node pkg=>`/`<composable_node pkg=>` 참조 전수 감사 추가** (§2-4는 find-pkg-share만 봤음) → 미빌드 5개 검출:
   - 빌드 추가 4개: lanelet2_map_visualizer(맵 표시), collision_detector(**기본 true 활성**), obstacle_collision_checker·predicted_path_checker(기본 false지만 값쌈) — finish_chain.sh packages-up-to에 명시
   - 의도적 미빌드 1개: **smart_mpc_trajectory_follower** — trajectory_follower_mode 기본값(trajectory_follower_node) 아님 + 빌드 시 무거운 Python 의존. 이 모드로 전환하려면 먼저 빌드 필요

## 1C. 9/2 낮 세션 — 심층 분석 → 재구성 (상세는 `개발계획_0902.md`, 보고서 `문서보관/검토보고_0902/`)

**결론**: 새벽의 "실기 모드 준비 완료"는 사실이 아니었음. 실기 모드는 (a) 브리지 토픽명 `_report`(Autoware는 `_status`) (b) launch_system:=false → vehicle_cmd_gate mrm 하트비트 대기·ADAPI engage 100% 거부 (c) 점군·점유격자 필수 구독 미충족 → trajectory 미발행 (d) 초기화 상태 부재, 4중으로 차가 못 움직이는 상태였고 psim은 이 구성을 검증하지 않음. 신호등도 TlRouter 100% 예외(iterparse clear 버그)·맵 방향 오배정 323개·lanelet당 3~10개 중복·state 4/6 영구 정지로 3층 전부 결함. run_real.py 기본 실행이 SCP로 VTD를 재로드(채점 세션 파괴).

**한 것 (전부 시뮬 PC 없이)**
1. **단일 워크스페이스 `hlfma_ws/` 재구성 + 전체 재빌드** (업스트림 복사본 + `src/hlfma/` 우리 패키지, `build_targets.txt`로 런타임 플러그인 51개까지 명시. ros2_ws·patches 폐지, ~/autoware는 잔재)
2. **autoware_launch 복사본 설정**: max_vel 12.8, stop_margin 1.0(신호등·정지선), turn_signal_search_time 4.0, AEB objects 기반, preset: goal_planner·회피 5종·virtual_traffic_light·obstacle_velocity_limiter·run_out 끔. `hlfma_vehicle_launch`(아이오닉6, `vehicle_model:=hlfma_vehicle`)
3. **브리지 재작성** (`bridge_node.py`+`osm_map.py`+`tl_router.py`): 토픽명, 초기화 상태, 빈 점군·격자, 워치독(0.4s→−3.0), 소켓 경합, 리스폰 감지·추정기 리셋·`/vtd/respawn`, 조향 보고 1차 지연, id 기반 yaw rate 예측(4s), 신호등 = "가라/서라"를 브리지가 결정해 다음 정지선 규제요소 전부에 GREEN/RED CIRCLE, state 6 상태기계, 매 프레임 전역 lanelet 판정, 5초 감시 로그. **단독 시험 통과**(`tools/test_bridge_alone.sh`: 20Hz·초기화 3·신호등 전환·89.8m 점프 감지·재접속·워치독 0.44s)
4. **route_node 신설** (`route_csv:=` 런치 인자, lanelet 방향 heading, goal 25m 너머, 막다른 lanelet 회피, 응답 로깅, auto_engage 기본 false). `start_autoware.sh` 재작성(mock/psim/실기, ROUTE_CSV/AUTO_ENGAGE/RVIZ, system 켬)
5. **mock_vtd 확장** (신호등 스케줄·리스폰·끊김·정속·전방 차량) + 회귀 `tools/regress_mock.sh` + 계약 점검 `tools/check_topic_contract.sh`
6. **자체 스택**: run_real.py SCP 경로 제거(`lab_restart_scenario.py`로 분리), 곡률 NaN 제거, V_DEFAULT 47, check_route.py 제어기 이관
7. **맵 v2** (`controller/out/livinglab_lanelet2_v2.osm` → 제어기 `map/lanelet2_map.osm`): elevation(z 35~79m), 신호등 접근로 단위 규제요소 1개(646→130), 정지선 object 기준·lanelet 끝, 방향 위반 0, turn_direction 353/353, 용접에서 의미 way 제외. `validate_semantics.py` 신설
8. Redmine 5건 답변 반영, xodr 막다른 차선 분석(`controller/out/map_edges.png`, junc21/2/5.png)

9. **mock 회귀로 실기 구성 첫 자율주행 성공** (`tools/regress_mock.sh`): route_example 852m 120초 완주, 최고속 46.8km/h, 지시등 동작, goal 정지. 출발 안 하던 원인 = **스폰 점이 lanelet 시작 경계(맵 경계)와 일치 → behavior_path `is_in_lanelet` 실패("Ego is out of route")** → 익스포터가 맵 경계 도로 끝 lanelet 34개를 3m 외삽(맵 v2 갱신). 토픽 계약 점검 0건(선택 입력 제외 규칙 포함)
10. **hlfma_ws 환경 고정**: `env.sh`(.bashrc가 source) + `colcon_defaults.yaml` + 폐쇄 밖 128개 COLCON_IGNORE(총 208) → `colcon build`·`source install/setup.bash`만으로 동작. `build_targets.txt`에 런타임 플러그인 51개 명시(1차 빌드에서 누락됐던 것). README.md
11. 맵 감사 2편(D 규약·E 유형스캔, `문서보관/검토보고_0902/`): 조건부 합격. 의심 449건·이미지 100장(`controller/out/suspects/`). lane_change 보수 규칙(실선 있으면 no) 적용. 실제 결함 잔여: 폭0~차폭미만 구간 전파 71건(E-1), real_route가 확폭 차선 14208로 진입(E-2), road 1927 3.45m 경계 순간이동(E-3)

**미검증(저녁 실기)**: 실기 E2E 전체, VTD ego z vs 맵 z(36.63/44.16), state 4 직진 중 발생 여부, 리스폰 후 route 유지·re-engage, objects 구성, 지시등 타이밍, 콜드부팅 시간.

## 2. 오늘(9/2 새벽) 한 것

### acados 정식화 (기술부채 A1 해소)
1. ansible role(`ansible/roles/acados`) 절차 그대로 로컬 설치: clone v0.5.3(+서브모듈) → cmake(QPOASES, PIC) → make install → t_renderer v0.2.0 → venv(casadi·sympy·acados_template)
   - 차이점 단 2개: 위치 `/opt/acados`→`~/acados` (CMake가 `ACADOS_SOURCE_DIR` env로 공식 지원), venv는 python3-venv 패키지 부재로 get-pip 부트스트랩 (임포트 검증 통과)
2. env 3곳 반영: `~/.bashrc`, `~/finish_chain.sh`(빌드), `start_autoware.sh`(런타임 LD_LIBRARY_PATH)
3. **path_optimizer·trajectory_optimizer COLCON_IGNORE 해제 → 빌드 성공**, `default_preset.yaml` motion_path_planner_type을 path_optimizer로 **원복** (수정분: common.param + vehicle_info 2곳만 남음, patch 재추출 완료)

### launch 참조 전수 감사 → 숨은 지뢰 9개 제거
4. 빌드된 모든 launch 패키지의 find-pkg-share 참조를 ignore 목록과 전수 대조 (50건 검출) → **활성 경로**(우리 launch 인자·preset 기본값 기준)에 있는 것만 선별 복원:
   - planning_evaluator (psim launch 파스 크래시 실증), control_evaluator (launch_control_evaluator 기본 true)
   - perception_online_evaluator (analytics publisher 기본 true), multi_object_tracker + map_based_prediction (psim object_recognition 기본 true)
   - probabilistic_occupancy_grid_map + 의존 체인 pointcloud_preprocessor·pcl_extensions·cuda_utils (scenario_simulation=false 기본 경로. CUDA 13.3로 GPU 커널 포함 빌드됨 — RTX 3070 sm_86 지원 확인)
   - 나머지 41건은 죽은 경로(perception/localization/system launch off, diffusion_planner 미사용 등) — 복원 불필요 근거 확인
5. ignore 장부 정리: 실측 79개 = 목록 79개 일치 (전 세션 누락 system_monitor 추가)

### 도구
6. `tools/psim_smoke.sh` — psim 헤드리스 스모크 (기동→노드/맵 검사→정리)
7. `tools/psim_e2e.sh` — psim 자율주행 E2E (initialpose→route→engage→속도 실측). **내일 아침 이것부터 돌리면 됨**
8. 고아 노드 정리 패턴 확립: `pkill -9 -f "[-][-]ros-args"` (자기매칭 회피 — 어제 삽질 재발 방지)

## 3. 오늘 알게 된 사실 / 판단

- **launch 파스는 lazy가 아님**: 조건이 참인 group 안의 find-pkg-share는 파스 시점에 즉시 해석 → 패키지 없으면 launch 전체 예외. ignore 처리는 "런타임에 그 경로를 타는가"가 아니라 "launch 인자·preset 기본값 기준 활성 경로인가"로 판단해야 함 (§2-4의 전수 감사가 그 작업)
- **adaptor류는 서비스 응답을 로깅 안 함**: goal 주입이 조용히 실패할 수 있음 → 디버깅은 ADAPI 서비스 직접 호출로 (응답에 사유 담김: "The planned route is empty" / "Goal is not valid")
- **goal pose의 heading은 그 지점의 차선 진행 방향**이어야 함 (다음 웨이포인트 방향 아님). 경로 주입 스크립트 구현 시 주의 — CSV 짝점(교차로 진입·진출)에서 heading은 진입 직전 주행 방향으로
- **mission planner는 goal 각도 검증함** (차선 방향과 불일치 시 거부). E2E에서 실증: 0.509rad(체인 방향) 거부 → 1.704rad(차선 방향) 성공
- `ros2 topic echo`는 대형 그래프에서 디스커버리 플레이크 있음 (127노드에서 15s 타임아웃 미스) → 판정은 AutowareState·operation_mode·실제 속도 같은 상태값으로
- route/state류 토픽은 transient_local → echo 시 `--qos-durability transient_local --qos-reliability reliable` 필요

## 4. 남은 것 / 다음 할 일

### 실기 (VTD 재연결 시 — 시뮬 PC 켜고)
- [ ] `tools/regress_mock.sh` 통과 상태에서 `./start_autoware.sh` 실기 E2E (통과 조건 동일: steering_status·trajectory·is_autonomous_mode_available·route SET·traffic_signals·계약 0·control_cmd·주행)
- [ ] 실측 5건: 신호등 state 타이밍·정지선 오차 / state 4 직진 중 발생 여부(→ state4_go 결정) / 리스폰 후 route·re-engage / objects 구성(정적물·보행자) / 지시등 타이밍. VTD ego z 초기값을 맵 z와 대조
- [ ] osgconv로 붉은 노면 폴리곤 추출(시뮬 PC OSG 또는 노트북 openscenegraph) → 맵 speed_limit 30 / 그 전 zone_speed_node
- [ ] 콜드부팅→control_cmd 시간 실측, `~/autoware/` 잔재 삭제(hlfma_ws 확정 후)
- [ ] 스폰부터 깨끗한 완주 1판, 신호등 state 실측 로깅, objects 장애물·보행자 확인 (todo0901 §3 실기 항목 그대로)

### 코드 (본선까지 핵심)
- [x] ~~경로 주입 스크립트~~ → **route_node**(9/2 낮). 남은 것: 리스폰 후 재주입 필요 여부(실측), 막다른 lanelet 회피 검증
- [ ] 자체 스택 항목들 (todo0901 §3 코드 항목 유지)

### 대회 준비 (todo0901에서 이월)
- [ ] 자동 로그인 설정 (콜드부팅→무조작)
- [ ] 사전테스트(9/3) 실측 3건: 경로 CSV 전달 매체 / 콜드부팅→주행 소요시간 / 평가 시스템 거동
- [x] Redmine 5건 답변 확인 (상단 ✅) → 후속: **xodr에서 붉은 노면(보호구역) 식별 요소 탐색** (시뮬 PC 불필요, controller/ 파서로 가능)

### 9/2 저녁 신규 (본선 전)
- [ ] **워크스페이스 정식화**: 빌드 불가(CUDA·TensorRT) 패키지만 무시하고 전체 빌드로 전환 → build.sh·build_targets.txt 제거 / acados를 /opt에 정식 설치(sudo, ansible 절차) → env.sh의 acados 줄 제거. 사전테스트 후 밤에
- [ ] 익스포터 E-1(폭 0.5m 미만 절단)·E-3(폭 계단 점프 s-분할) → 재익스포트·감사 재실행. check_route.py에 road 1926/1927·북동 미선언 교차 구역·폭<2m/막다른 lanelet 근접 경고 추가
- [ ] 4.9km 실경로 mock 주행(차로변경·포켓 진입·14208) 결과 반영

### 9/2 낮 신규 (본선 전)
- [ ] 횡단보도 crosswalk lanelet 197곳 출력 (xodr object) → 규정 10·12
- [ ] state 6 상태기계·state4_go 실측 검증, 임시방편 7(is_simulation) 결정
- [ ] diagnostic graph 정식화(임시방편 2), 자동 로그인(gdm3, sudo), rviz off 본선 변형 리허설
- [ ] 자체 스택: real_route_path1 경로 폴리라인 되꺾임(cusp) 37곳 → lane_graph 접합 조사 (NaN은 해결, 7~15km/h 감속 잔존)
- [ ] 맵 v2 애매 케이스: turn_direction 다중 후보 6곳(straight로 절충), 교차로 내부 신호등 기둥 2개(controller 3·312) 원저작 의도 확인, 정지선 object 미채택 11곳(signal.s 폴백)

## 5. 기술부채 장부 (todo0901 §5 대비 변동)

### 해소됨
- ~~A1 path_optimizer→path_sampler 임시 전환~~ → **acados 정식 설치·원복 완료**
- ~~B9 install 잔재 오염~~ → 전수 대조 검증 (stale 0)
- A2 업스트림 수정 patch 백업 → **재추출 완료** (preset 원복 반영, 이제 common.param + vehicle_info만)
- A3 COLCON_IGNORE 일괄 88개 → **79개로 감소 + 전수 감사로 "launch 활성 경로 무결" 실증** (psim 한정. 근거가 '일괄 추정'에서 '개별 검증'으로 격상)

### 유지 (변동 없음, 원복법은 todo0901 §5 참조)
- A4 launch_system:=false에서 engage 가능 여부 — **실기 모드에서만 해당** (psim은 system 켜진 채 engage 성공 실증). VTD 연동 시 1순위 확인
- A5 tier4_perception/sensing/localization_launch 미빌드 (우리 launch 인자로는 include 안 됨 — psim 검증에서 문제 없음 실증)
- B6 is_simulation:=true 전수조사 미완 / B7 더미 pcd / B8 stderr 통과 패키지 (오늘 추가분 stderr는 전부 ROS deprecation·CMake 정책 경고 — 무해 확인)
- C10~13 브리지 근사치들 / D14 ansible 미실행(acados는 오늘 해소, ccache·geographiclib 잔여) / D15 systemd 정식화

### 9/2 낮 정리
- ~~A2 patches~~ → 폐지 (설정이 hlfma_ws/src/hlfma/autoware_launch 실제 파일), ~~A4 launch_system:=false engage~~ → 불가능 확정, system 켬(psim 구성)으로 해소, B7 더미 pcd 유지, ~~C10 steering=명령값~~ → 1차 지연 근사, C11 속도 차분 → 점프 리셋 추가(본질적 근사), ~~C12 TlRouter 미검증~~ → 재작성·단독 시험 통과(실기 미검증), D15 systemd 미착수
- **임시방편 장부는 `개발계획_0902.md` §4가 정본** (빈 점군·격자 확정 / dummy_diag / state 4 / z=0 해소 / zone_speed / is_simulation)

### 신규
- **venv get-pip 부트스트랩** (`~/acados/.venv`) — python3-venv 패키지 부재 우회. 기능 검증 완료라 실해 없음. 정식화 원하면: `sudo apt install python3.12-venv` 후 venv 재생성 (선택사항, 급하지 않음)
- **OGM CUDA 커널 빌드** — CUDA 13.3 + gcc 13.3 조합으로 컴파일됨. 실기에서 OGM은 미사용 경로(launch_perception=false)라 리스크 없음, psim에서만 동작. 이상 시 해당 노드만 의심

## 6. 다음 세션 시작 절차

1. `대회정보.md` → 이 문서 (Redmine 5건은 답변 완료, 신규 질의 없음)
2. 회귀: `cd ~/2026-HL-FMA-VTD && bash tools/regress_mock.sh` (mock VTD + 실기 구성 전체, 약 5분) → 전부 PASS면 스택 건강. psim은 `./start_autoware.sh psim`
3. GUI로 보려면: `./start_autoware.sh psim` (rviz 포함) → rviz에서 2D Pose Estimate / 2D Goal Pose
4. VTD 연동: 시뮬 PC 켜고 케이블 연결 → `./start_autoware.sh` (route_config.yaml의 CSV 자동 주입, rviz 확인 후 수동 engage) — **실기 미검증, §4 첫 항목**
