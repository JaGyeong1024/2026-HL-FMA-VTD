# This file is managed by sync-params workflow.
# DO NOT EDIT THIS FILE DIRECTLY FOR ADDING OR REMOVING FIELDS. Follow these steps:
#
# 1. Modify the param file in the upstream repository.
# 2. Run the sync-params workflow or run `python .github/scripts/sync_params.py control` to update this file.
# 3. If local changes are needed, annotate them with an override marker (see below).
#
# Keys marked with '{OVERRIDE}' or '# {OVERRIDE: reason}' are persisted as variant overrides.
# 'reason' can be any single-line text without braces.
# To keep local changes, annotate fields with '# {OVERRIDE}' or '# {OVERRIDE: reason}'.
#
# Source: https://github.com/autowarefoundation/autoware_universe/blob/26e3c8e411fd439965a957fb19cdf083b8b24828/control/autoware_vehicle_cmd_gate/config/vehicle_cmd_gate.param.yaml

/**:
  ros__parameters:
    update_rate: 10.0
    system_emergency_heartbeat_timeout: 0.5
    use_emergency_handling: true
    check_external_emergency_heartbeat: $(var check_external_emergency_heartbeat)
    enable_cmd_limit_filter: true
    filter_activated_count_threshold: 5
    filter_activated_velocity_threshold: 1.0
    external_emergency_stop_heartbeat_timeout: 0.0
    stop_hold_acceleration: -1.5
    # HL FMA 9/10: 급정지를 '계획된 감속'이 아니라 '물리적 풀 브레이크'로 본다.
    #   브리지 패킷은 -6.0 까지 받는데(ACCEL_MIN) 비상 경로가 -2.4 라 12.8m/s 정지거리가
    #   40m 였다. -5.5 면 21m. 채점 15항목에 승차감·가속도 기준이 없어 감점 위험은 없다.
    #   0.61g 수준으로 마른 노면 타이어 한계(~1.0g) 안이다. 되돌리려면 -2.4
    emergency_acceleration: -5.5
    moderate_stop_service_acceleration: -1.5
    stop_check_duration: 1.0
    nominal:
      vel_lim: 25.0
      reference_speed_points: [0.1, 0.3, 20.0, 30.0]
      steer_cmd_lim: [1.0, 1.0, 1.0, 0.8]
      steer_rate_lim_for_steer_cmd: [1.0, 1.0, 1.0, 0.8]
      lon_acc_lim_for_lon_vel: [5.0, 5.0, 5.0, 4.0]
      lon_jerk_lim_for_lon_acc: [80.0, 5.0, 5.0, 4.0]  # The first element is required for quick pedal changes when stopping and starting.
      lat_acc_lim_for_steer_cmd: [5.0, 5.0, 5.0, 4.0]
      lat_jerk_lim_for_steer_cmd: [7.0, 7.0, 7.0, 6.0]
      lat_jerk_lim_for_steer_rate: 10.0
      steer_cmd_diff_lim_from_current_steer: [1.0, 1.0, 1.0, 0.8]
    on_transition:
      vel_lim: 50.0
      reference_speed_points: [20.0, 30.0]
      steer_cmd_lim: [1.0, 0.8]
      steer_rate_lim_for_steer_cmd: [1.0, 0.8]
      lon_acc_lim_for_lon_vel: [1.0, 0.9]
      lon_jerk_lim_for_lon_acc: [0.5, 0.4]
      lat_acc_lim_for_steer_cmd: [2.0, 1.8]
      lat_jerk_lim_for_steer_cmd: [7.0, 6.0]
      lat_jerk_lim_for_steer_rate: 10.0
      steer_cmd_diff_lim_from_current_steer: [1.0, 0.8]
