# hlfma_vehicle_launch

HL FMA 대회 차량(아이오닉 6) 패키지. Autoware `sample_vehicle_launch`를 복사해 이름을 바꾸고
`hlfma_vehicle_description/config/vehicle_info.param.yaml`에 대회 차량 제원을 넣은 것.
기동 시 `vehicle_model:=hlfma_vehicle`.
